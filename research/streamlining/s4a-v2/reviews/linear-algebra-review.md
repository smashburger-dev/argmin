# Domänenreview linear-algebra (S4A-v2)

- **Shard:** `research/streamlining/s4a-v2/shards/linear-algebra.json` (26 Entries, 13 Familien: 7 Registry + 6 neue)
- **Datum:** 2026-09-03
- **Reviewer:** unabhängiger Domänenreviewer (nicht der Implementer), adversarial
- **Prüfungsumfang:** `family-model.md` (Stand 2026-09-02), `taxonomy.schema.json` (überflogen), `archetypes.json`, `canonical-families.json`, `inputs/linear-algebra-v1.json`, Shard inkl. aller Rationales; Quellbelege aus `content/exercises/w05.json`, `content/exercise-definitions/linear-algebra/*.json` (column-choice, gauss-operation-choice, rank-system-debug, shape-debug), `assets/js/core/graders.js` (gradeChoice, gradePair, parsonsDiagnosis, gradeSympyExpression-Nähe), `content/competencies/core.json`; für domänenübergreifende Erwartungen die übrigen Shards (lesend, nur Aggregatzeilen des Assemblers).

## Verifikationsläufe (eigene Skriptauswertungen)

- `node research/streamlining/s4a-v2/scaffold-shard.mjs --validate linear-algebra` → **bestanden**: „Eintraege 26/26, ID-Mengen exakt, Schema valide, TODO-Platzhalter 0", Exit 0.
- `node research/streamlining/s4a-v2/selftest.mjs` → **27/27 bestanden** (kein Fehlschlag; der im foundations-Review noch offene globale Reconcile-Check läuft im aktuellen Gesamtstand durch).
- `node research/streamlining/s4a-v2/assemble-v2.mjs` (Trockenlauf) → meldet 94 Registry-Verletzungen (erwarteter Vor-Reconcile-Stand, Block D1). Linear-algebra-relevant davon: `transform-expression-simplify-canonical` erwartet 7 / deklariert 3 (w01-e2, f-algebra-final-boss-01, w05-e5); `trace-assignment-state` 14/23 (Übererfüllung aus anderen Shards; linalg-Anteil nur w05-e15); `transform-system-2x2-elimination` 6/5 (f-linalg-final-boss-01 fehlt, s. F3); `transform-rank-dependence-rowops` 4/1 (w05-e10); `validate-shape-contract` 12/abweichend (in den 74 weiteren Zeilen); plus 6 neue linalg-Familien ohne Registry-Eintrag.
- Eigene `node -e`/`python3`-Auswertungen gegen v1/Shard/Registry/Archetypen/Grader: ID-Mengen, ruleIndex-Deckung (31/31), uncertainty-/humanReview-Zeichentreue (vollständig), byte-genaue Registry-Verträge (7/7 Registry-Familien), authorityMode-Abbildung (seeded exakt bei den 5 `seed-generator`-Quellen), Archetyp-/Adapter-Mapping (26/26), difficulty-/caseId-Treue (26/26), competencyClaim gegen `core.json` (46 Kompetenzen, alle Claims gültig), caseId-Eindeutigkeit je Familie×Archetyp, Token-Multiset-Kollision der 6 neuen Familien-IDs (keine).

## Findings

| ID | Schweregrad | sourceIds | verletzte Regel | Beleg (Datei + Fundstelle) | kleinste Korrektur |
|---|---|---|---|---|---|
| F1 | **high** | f-linalg-column-choice-01 | Alle drei Mitgliedschaftstests (family-model §4 Tests 1–3): Prozedur-, Referenzmodell- und Diagnosetest in `transform-system-2x2-elimination` | Familienvertrag im Shard: solutionPath „Eliminationswahl treffen, eliminieren, per Rückeinsetzen auflösen und das Lösungspaar angeben", referenceModel „2×2-Linearssystem mit eindeutigem Lösungspaar (Eliminationsstrategie, Rückeinsetzen)", einzige Hypothese „Eliminationswahl statt Vorzeichenfehler" (`shards/linear-algebra.json`, Eintrag f-linalg-column-choice-01). Quelle dagegen: fullSolution „$a_1+a_2=(2+1,1+3)^T=(3,4)^T=b$. Daher gilt $(x_1,x_2)=(1,1)$" — reine Verifikation durch Nachrechnen, keine Eliminationswahl, kein Rückeinsetzen, Antwort ist eine Choice (both-one), kein Paar; typicalErrors „Zielvektor mit Koeffizienten verwechseln / nur eine Spalte berücksichtigen" (`content/exercise-definitions/linear-algebra/column-choice.json`). v1 hatte folgerichtig getrennte Familien vorgeschlagen (`linear-algebra-column-combination-coefficient-choice` vs. `-solve`; `inputs/linear-algebra-v1.json`, f-linalg-column-choice-01). | f-linalg-column-choice-01 aus `transform-system-2x2-elimination` herauslösen in eine eigene (Singleton-)Familie vom Typ Erkennen-von-Koeffizienten mit Konzeptsystem-Referenzmodell (Spaltenbild), candidater Familienvertrag aus Lösungsweg (nachrechnen + begründen), Invarianten und Choice-Grammatik begründen; Council beschließt neue Familie und reconciled `transform-system-2x2-elimination` 6→4 (zusammen mit F3-Split 6→5). f-linalg-column-vector-01 bleibt: „2×2-System ablesen, lösen, durch Einsetzen verifizieren" besteht alle drei Tests. |
| F2 | **medium** | w05-e9 | Multi-Archetyp-Regel §4 („zulässig, wenn Lösungs- **und** Evidence-Vertrag identisch bleiben") + Diagnosetest (§4 Test 3) | `formula-scalar-product` vereint 5× `numeric-exact/deterministic` und 1× `output-predict-lines` mit 1× `rationale-note/manual-rubric` (w05-e9; `shards/linear-algebra.json`). rationale-note ist vertraglich nicht-autoritativ (Freitext, „Selbsteinschätzung gegen die Rubrik; kein automatisierter Vergleich"; v1 answerContract w05-e9) — der Evidence-Vertrag ist gegenüber den numerischen Mitgliedern nicht identisch; keine Feedbackgrammatik kann die Familien-Hypothesen je an freiem Begründungstext auswerten. Sichtbar auch an der Review-Policy: nur w05-e9 trägt `reviewEligible: false` in einer sonst vollständig review-fähigen Familie. v1 hatte eine eigene Rubrik-Familie vorgeschlagen (`linear-algebra-matrix-product-definition-rubric`). Spiegelbild zu foundations-F3 (dort reflect-Familie). | Council-Entscheid: entweder w05-e9 in eine eigene Rubrik-Singleton-Familie (Linie des v1-Vorschlags) verschieben, oder dokumentierte Ausnahme (rationale-note als bewusst nicht-grammatikfähiges Mitglied, da nur 2 Quellen dieser Archetypklasse im Korpus) in die Registry aufnehmen. Kein stilles Belassen. |
| F3 | **low** (Info, Empfänger Council, dem Shard nicht angelastet) | f-linalg-final-boss-01, w05-e5, w05-e10, f-linalg-shape-debug-01, f-numpy-shape-predict-01, w05-e15 (+ 6 neue Familien) | Registry-Reconcile-Pflicht §5 („jede erwartete Anzahl gleich der abgeleiteten"); `hypothesisMemberSourceIds` sind nicht-authoritative Starthilfen | Assembler-Trockenlauf (s. oben): `transform-system-2x2-elimination` 6→5, weil der Shard f-linalg-final-boss-01 zu Recht in die neue Familie `construct-linalg-contract-synthesis` stellt — Drei-Vertrags-Synthese mit hidden-tests (v1 solPath f-linalg-final-boss-01) besteht den Prozedurtest der Eliminationsfamilie nicht; der Registry-Hypo-Eintrag war falsch, der Split ist fachlich korrekt. Ferner: `transform-expression-simplify-canonical` 7→3, `transform-rank-dependence-rowops` 4→1, `validate-shape-contract` 12→abgeleitet, `trace-assignment-state` 14→23 (linalg-Anteil nur w05-e15, korrekt zugeordnet); 6 neue linalg-Familien ohne Registry-Eintrag. Für keine dieser Lücken liegt eine linalg-Quelle falsch; `formula-scalar-product` 7→7 und `classify-matrix-shape` 1→1 treffen exakt. | Orchestrator/Council: Registry-Erwartungen nach den abgeleiteten Mitgliedschaften reconcilieren (§5), 6 neue Familien formal beschließen (Einzelfall F1/F2 beachten: Verschiebungen ändern die Counts erneut). Keine Korrektur am Shard erforderlich. |

Keine critical-Findings. Kein Persistenz-, ID-Sicherheits- oder Scheinkonsum-Verstoß.

## Abgearbeitete Prüfliste

1. **ID-Abdeckung & Zeichentreue:** 26/26 sourceIds mengengleich mit v1, keine Duplikate, keine Extra-/Fehl-IDs; `currentDefinitionId` 26/26 identisch; `legacyWeekId`/`sourceKind` korrekt (16 legacy-week-exercise, 10 canonical-definition). **Bestanden.**
2. **ruleIndex-Deckung:** Alle 31 v1-Feedbackregeln exakt je Quelle dispiert (0…n−1, keine Duplikate, keine Lücken). Verteilung: 18 grader-predicate (alle aus den 10 fully-consumed-Quellen), 12 answer-note + 1 input-validation (alle aus den 9 not-consumed-Quellen), 7 no-rules-Quellen mit leerer Disposition. Konsumstatus-Kreuzcheck sauber (0 verbrauchte Regeln auf nicht-prädikativen Pfaden, 0 unkonsumierte als grader-predicate). **Bestanden.**
3. **Uncertainty-Dispositions:** Feld/Level/Reason 1:1 zeichengetreu aus v1 bei allen 26 Entries (29 Dispositionen). Auflösungen: 13× resolved (7 source-evidence, 6 code-evidence), 5× requires-noa-decision/deferred (w05-e3, w05-e15 lessonIds → S4B; w05-e11/12/13 targetAction; w05-e7 doppelt) mit vollständigen owner/decision/evidence/latestGate. Kein `resolved-by-review-council`. **Bestanden.**
4. **Human-Review-Dispositions:** required-Flag und reasons 26/26 zeichengetreu (4× required: w05-e7, w05-e11/12/13 mit Merge-/Numbas-Begründungen); offene mit owner/decision/evidence/latestGate (deferred-to-domain-migration, requires-noa-decision); keine vorgetäuschte menschliche Freigabe, kein council-Bezug. **Bestanden.**
5. **Fachliche Kohärenz je Familie (drei Tests):** Überwiegend überzeugend: `formula-scalar-product` für die 5 numerischen Mitglieder + w05-e16 (Zeilenprodukte [3,−1] sind zeilenweise Skalarprodukte; parametrische Variation über Zeilen; v1 solPath w05-e16) und den geseedeten Kern f-linalg-matmul-entry-01; `construct-matvec-shape-contract` (w05-e8 implementieren vs. w05-e14 ordnen: gemeinsame Solution-Schablone Normalisierung→Assert→Rückgabe, gemeinsames matvec-Referenzmodell — Antwortform ist explizit keine Grenze, §4; Präzedenz foundations-Frage-2); `validate-shape-contract` (Vertragsregel auf Shapes anwenden: Verletzung bestimmen vs. Ergebnis-Shape folgern); Trennungen w05-e4/det2 (Vielfach-Erkennung vs. Determinante), w05-e10/rank-debug (numerische Elimination vs. Diagnose aus fertiger Stufenform) und w05-e16/shape-predict (numerische Produkte vs. Broadcasting-Shape) jeweils mit bestandenen Trennungstests. Ausnahmen: **F1 (column-choice), F2 (w05-e9)**. **Teilweise beanstandet.**
6. **Zu breite/zu enge Familien:** `formula-scalar-product` (7) und `transform-system-2x2-elimination` (5) sind die einzigen Mehr-Archetyp-Flächen; beide Auffälligkeiten sind F1/F2. Zu enge Familien: keine — die 5 neuen Singletons sind je einzeln begründet (Punkt 14); der final-boss-Split aus der Registry-Hypothese ist korrekt (F3). **Beanstandet nur via F1/F2.**
7. **Registry-Verträge byte-genau:** Für alle 7 Registry-Familien sind summary, solutionPath, referenceModel zeichengetreu und errorHypotheses mengengleich übernommen; familienintern bei jedem Mitglied identisch; familyGroup stimmt. 6 neue Familien mit vollständigen, kohärenten Verträgen und passender familyGroup (classify-concept ×3, formula-apply ×1, construct-program ×2); keine stillen Namensvarianten (Token-Multiset-Kollision gegen alle 64 Registry-IDs geprüft: keine). Reconciliation steht aus (F3). **Bestanden.**
8. **TaskArchetype:** 25/25 atomic korrekt aus activityType/graderId abgeleitet (numeric-exact 7, choice-diagnose 6, tuple-exact 4, code-test 2, output-predict-lines 2, expression-equivalence/state-trace-vars/program-ordering/rationale-note je 1); alle aktiv, `graderAdapter` 25/25 identisch mit Archetyp-Adapter; Composite nennt nur `numbas-exam-retired`. Multi-Archetyp-Familien: 4 (scalar-product, elimination, matvec-shape-contract, shape-contract); 2 vertretbar, 2 beanstandet (F1/F2). **Bestanden mit Ausnahmen F1/F2.**
9. **Parametrische Variation & caseIds:** Alle 25 caseIds zeichengetreu = v1 `proposedCaseType`; keine Duplikate innerhalb Familie×Archetyp; keine Pseudo-Falltypen; Fallunterscheidungen (fix vs. seeded, Eintragspositionen, Substitutions- vs. Additions-Elimination, taskMode-Analoga) als Fallparameter modelliert. `authorityMode`: 5× seeded exakt deckungsgleich mit v1 `solverContract.kind=seed-generator` (f-linalg-column-vector-01, f-linalg-det2-01, f-linalg-matmul-entry-01, f-numpy-shape-predict-01, f-linalg-solve-system-01), 20× static — kanonische Abbildung §4 fehlerfrei, inkl. des familyContractVariant-Paars w05-e1 (static, reference-solver) / f-linalg-matmul-entry-01 (seeded). **Bestanden.**
10. **Fehlerhypothesen & Feedback-Zielpfade (kein Scheinkonsum):** Gegen `assets/js/core/graders.js` verifiziert: gradeChoice liest nur `choice ===/!== 'id'` (graders.js-Zeilen 79–95) — die drei if-Muster (`zero-row`, `contradiction`, `sum-all` in gauss-operation-choice/rank-system-debug/shape-debug-Definitionen) werden daher zu Recht als answer-note geführt; gradePair liefert swapped hartkodiert und kennt kein noninteger (Zeilen 96–115) — beide tuple-Regeln zu Recht answer-note mit pair-swapped/pair-noninteger-Formalisierung; parsonsDiagnosis feuert `order-length-mismatch` nur als reine Zählanomalie (Zeilen 300–313) — w05-e14 korrekt grader-predicate; gradeSympyExpression liest keine feedbackRules — w05-e5 korrekt answer-note + input-validation (Zeichengate als format_errors-Analogon). **Bestanden** (Familienabdeckung ausgenommen F1/F2).
11. **Difficulty-Monotonie:** 26/26 = v1 `proposedDifficultyProfile`; innerhalb der Familien keine invertierten Profile (scalar-product: basic×5 für statische Instanzen, core×2 für Rubrik- und Seed-Variante; elimination: core×4 + basic für die Choice-Erkennungsaufgabe — nach F1-Verschiebung homogen core; matvec: core für implementieren, basic für ordnen). **Bestanden.**
12. **Primary Claim vs. coEvidence:** primary 26/26 ∈ v1 competencyIds und ∈ `core.json` (46 Kompetenzen); coEvidence 15 Einträge, alle ⊆ v1 competencyIds und ⊆ core.json (u. a. w05-e8 c-numpy-basics/c-linalg-matrices, w05-e14–16 mit c-python-reading, final-boss mit 4 Co-Evidenzen). Anmerkung ohne Finding: w05-e3 trägt primary c-linalg-independence bei reiner Skalarprodukt-Rechnung und Primärlektion l-linalg-matrices — als `deferred-to-s4b` mit Noa-Entscheidungsauftrag korrekt disponiert, S4B muss den Claim bei der Bindung bestätigen. **Bestanden.**
13. **Persistenz & ID-Sicherheit:** 22× preserve-id (korrekt), 3× merge-map-required (w05-e11→`transform-system-2x2-elimination`, w05-e12/w05-e13→`formula-scalar-product`, alle mit nicht-leerem mergeInto; eigene caseIds als extrahierte Falltypen erhalten), 1× retire-blocked (w05-e7, s. Punkt 15). Keine abgeleitete ID wird Attempt-Schlüssel. **Bestanden.**
14. **Singleton-Begründungen:** 5 neue Singletons (classify-independence-multiple, formula-det2-independence, classify-row-operation-validity, classify-rank-solution-case, construct-linalg-contract-synthesis) mit vollständigen Verträgen; Trennungsrationales liegen vor (w05-e4 vs. det2: Prüfwerkzeug Vielfaches vs. Determinante; w05-e10 vs. rank-debug: numerische Elimination vs. Stufenform-Diagnose); det2/Gauss/rank-debug/final-boss folgen v1-Einzelvorschlägen mit eigenem Konzept- bzw. Synthese-Referenzmodell (Präzedenz foundations-Frage-4: vollständige Verträge erfüllen die Begründungspflicht). 2-Mitglieder-Neufamilie construct-matvec-shape-contract begründet (Punkt 5). **Bestanden.**
15. **Composite-Entscheidung (Pflichtgegenfall w05-e7):** composite-placement, status retired, `historicalArchetypeId: numbas-exam-retired`, 3 atomare Komponenten (w05-e1 Produkteintrag, w05-e5 Termvereinfachung, w05-e6 2×2-System) mit je eigener role-Beschreibung, keine Komposit-Komponente, kein Selbstbezug; sequenceContract (geführte Dreiteilung, verzögerte Musterlösung) und partialScoreContract (Numbas-Teilscore 0–4, 50-%-Grenze, updateScore ohne Runtime-Zielpfad) dokumentieren die lokale Semantik statt sie wiederauferstehen zu lassen; `persistence.note` nennt die explizite S1B-Freigabe; `reviewEligible: false`; `feedbackDispositions: []` bei 0 Regeln korrekt. Einziger weiterer Composite-Kandidat: keiner (alle übrigen Quellen sind echte Einzelaufgaben; verifiziert gegen w05.json-Typen). **Bestanden.**

## Stellungnahme zu offenen Punkten (kein separater Implementer-Fragenkatalog im Shard)

Der Shard enthält keinen eigenen Fragenabschnitt; die offenen Punkte liegen in den Dispositionen und sind durchweg korrekt verortet:

1. **w05-e11/w05-e12/w05-e13-Merges (`requires-noa-decision`):** Zustimmung. Additionstechnik (w05-e11) und Eintrags-/Vorzeichenvarianten (w05-e12/13) sind echte Fallunterschiede, die als eigene caseIds erhalten bleiben; ob die IDs per Merge-Map oder Alias fortleben, ist eine Noa-Freigabe mit Persistenzfolge — genau der verzeichnete Entscheidungsauftrag. mergeInto vorab fixiert, Attempts bleiben lesbar. Kein Stillbruch.
2. **w05-e3/w05-e15-Lektionszuordnung (`deferred-to-s4b`):** Zustimmung. Beides sind echte Platzierungsfragen (Matrizen- vs. Independence-Lektion; Spaltenbild- vs. Tracing-Lektion), keine Familienfragen — S4B ist das richtige Gate, S4B muss dabei den w05-e3-Claim (Punkt 12) mitentscheiden.
3. **w05-e7 (`requires-noa-decision` + `deferred-to-domain-migration`):** Zustimmung. Ob Sequenz- und Score-Semantik mit den dokumentierten Verträgen abschließend extrahiert sind, kann nur Noa entscheiden; der exerciseId-Alias braucht die Domainmigration. Retire-Seite ist vertragskonform abgeschlossen.
4. **Registry-Hypothese `transform-system-2x2-elimination` (6 inkl. final-boss):** Der Shard weicht zu Recht ab (F3) — die Hypothese war fachlich falsch, der Split ist kein Implementer-Eigenmächtigkeit, sondern Prozedurtest-Konsequenz. Council bestätigt.
5. **Registry-Hypothese `formula-scalar-product` (7 inkl. w05-e9/w05-e16):** Mengenmäßig erfüllt (7→7), aber die Hypothese trägt zwei der drei beanstandeten Strukturprobleme (F1 liegt in der Nachbarfamilie aus derselben Hypo-Logik, F2 in dieser). Council sollte bei der Reconciliation nicht nur zählen, sondern die F1/F2-Verschiebungen mitbeschließen.

## Gesamturteil

**Coral (nachbesserungsbedürftig, kein Blocker).**

Begründung: Die maschinenverifizierbare Struktur ist vollständig in Ordnung — 26/26 IDs, exakte ruleIndex-Deckung aller 31 Regeln, zeichentreue uncertainty-/humanReview-Übernahmen, byte-genaue Registry-Verträge, korrektes Archetyp-/Adapter-/authorityMode-/difficulty-/caseId-Mapping, gültige Claims gegen `core.json`, ehrliche Feedback-Zielpfade ohne Scheinkonsum (gegen `graders.js` verifiziert), vertragskonformer Pflichtgegenfall w05-e7 mit S1B-Note, Merge-Maps mit nicht-leerem mergeInto. Verhindert eine grüne Freigabe: **F1** — f-linalg-column-choice-01 (reine Erkennungsaufgabe, fullSolution durch Nachrechnen) verletzt Prozedur-, Referenzmodell- und Diagnosetest in der Eliminationsfamilie; dazu **F2** — w05-e9 trägt als nicht-autoritative rationale-note einen anderen Evidence-Vertrag als seine numerische Familie (eigene Review-Policy inklusive). Beide Korrekturen brauchen Council-Beschlüsse (neue Familien, Count-Reconcile), kein bloßes Shard-Patchen. F3 ist reine Council-Integrationsmaterie (D1) und dem Shard nicht angelastet.

**Integrationarbeit für den Parent/Council (Block C/D1):** F1-Verschiebung (neue Familie, elimination 5→4 bzw. 6→4 inkl. F3-Split), F2-Entscheid (eigene Rubrik-Familie oder dokumentierte Ausnahme), F3-Reconcile (7→3, 4→1, 12→abgeleitet, 14→23, 6 neue Familien beschließen), S4B-Notiz w05-e3-Claim.
