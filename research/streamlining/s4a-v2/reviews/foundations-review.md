# Domänenreview foundations (S4A-v2)

- **Shard:** `research/streamlining/s4a-v2/shards/foundations.json` (49 Entries, 25 Familien: 18 Registry + 7 neue)
- **Datum:** 2026-09-02
- **Reviewer:** unabhängiger Domänenreviewer (nicht der Implementer), adversarial
- **Prüfungsumfang:** `family-model.md` (Stand 2026-09-02), `taxonomy.schema.json`, `archetypes.json`, `canonical-families.json`, `inputs/foundations-v1.json`, Shard inkl. aller Rationales; Quellbelege aus `content/exercises/w01.json`–`w04.json`, `content/exercise-definitions/foundations/*.json`, `assets/js/core/graders.js`, `assets/js/core/foundations_fresh_generators.mjs`, `assets/js/core/w01_generators.mjs`; für die domänenübergreifenden Erwartungen sämtliche sechs anderen Shards und v1-Inputs (lesend).

## Verifikationsläufe (eigene Skriptauswertungen)

- `node research/streamlining/s4a-v2/scaffold-shard.mjs --validate foundations` → **bestanden**: „Eintraege 49/49, ID-Mengen exakt, Schema valide, TODO-Platzhalter 0", Exit 0.
- `node research/streamlining/s4a-v2/selftest.mjs` → 26/27 bestanden; der eine Fehlschlag („Assembler-Hauptlauf ohne Shards …") ist ein globaler Registry-Reconcile-Check, ausgelöst durch die Shards gesamt, nicht foundations-spezifisch (Details F7).
- `node research/streamlining/s4a-v2/assemble-v2.mjs` (Trockenlauf) → meldet 94 Registry-Verletzungen; foundations-relevant davon: `transform-expression-simplify-canonical` erwartet 7 / deklariert 3; `validate-required-field-raise` 13/5; `aggregate-accumulator-count` 12/4; `validate-test-design-coverage` 4/2; `construct-guarded-loop` 4/2; `classify-control-construct` 2/1; `trace-assignment-state` 14/23 (Übererfüllung durch andere Shards); `aggregate-validate-and-count-records` 4/8 (research-capstone); plus 7 neue foundations-Familien ohne Registry-Eintrag.
- Eigene `node -e`-Auswertungen gegen v1/Shard/Registry/Archetypen: ID-Mengen, ruleIndex-Deckung, uncertainty-/humanReview-Zeichentreue, byte-genaue Registry-Verträge, authorityMode-Abbildung, Archetyp-/Adapter-Mapping, difficulty, competencyClaim, placement, caseId-Vergabe (Ergebnisse in Prüfliste).

## Findings

| ID | Schweregrad | sourceIds | verletzte Regel | Beleg (Datei + Fundstelle) | kleinste Korrektur |
|---|---|---|---|---|---|
| F1 | **high** | w02-e1 (f-code-reading-output-01 angrenzend) | Diagnosetest (family-model §4 Test 3): mengen-identische errorHypotheses, deren Feedbackgrammatik das Mitglied abdeckt | w02-e1-Regeln #0/#1 sind Slice-Grenzen-Diagnosen („s[3:8] endet vor Index 8", „s[11:14] greift auf Indizes 11,12,13"; `inputs/foundations-v1.json` feedbackOccurrences, `content/exercises/w02.json`). Die Familien-Hypothesen von `trace-assignment-state` sind `reads-new-value, overwrite-forgotten, print-order, state-object-confusion` (`canonical-families.json`) — keine Achse deckt Slice-Grenzen-Fehler. Die Slice-Achse existiert ausgerechnet in `trace-collection-state` („Index- oder Slice-Grenzen falsch gezogen"). | w02-e1 aus `trace-assignment-state` herauslösen; zielführendstes Ziel: `trace-collection-state` (solutionPath „Collection-Operation Schritt für Schritt anwenden und Zustand angeben" deckt Sequenz/Slice-Endzustand; Strings sind Sequenzen) mit Registry-Anpassung (expected 1→2, Hypothesen bleiben gültig). Alternativ Council-Begründung dokumentieren, warum die Slice-Regeln als answer-notes außerhalb der Familiengrammatik bleiben dürfen. |
| F2 | **medium** | w01-e6 | Prozedurtest (§4 Test 1) + Grenzkonsistenz `trace-assignment-state` vs. `trace-call-composition` | `content/exercises/w01.json` w01-e6: Snippet besteht nur aus zwei `def` und `print(flaeche(3, 4), umfang(3, 4))` — **keine einzige Zuweisung**; solutionPath von `trace-assignment-state` („Zuweisungen in Ordnung auswerten") ist auf w01-e6 nicht anwendbar. Zugleich hat w01-e5 (in `trace-call-composition`) sehr wohl Zuweisungen (`ergebnis = kombiniere(4,3)`), w01-e6 aber nicht — die Grenze liegt invertiert zur Vertragsprosa. Beide Quellen sind Funktions-Trace; die Zuordnung wirkt nach Oberfläche (predict vs. state-trace), was explizit keine Familiengrenze ist (§4). | w01-e6 → `trace-call-composition` verschieben (expected 2→3, Council) ODER Registry-prosa von `trace-assignment-state` so präzisieren, dass aufrufende print-Zeilen mit stdout-Puffer ausdrücklich eingeschlossen sind. Beides Orchestrator/Council; kein foundations-Stillschreiben. |
| F3 | **medium** | w01-e11, f-meta-error-log-01 | Multi-Archetyp-Regel §4: „zulässig, wenn Lösungs- **und** Evidence-Vertrag identisch bleiben"; Diagnosetest für nicht-grammatikfähiges Mitglied | `reflect-error-journal-rationale` vereint `rationale-note/manual-rubric` (w01-e11) mit `choice-diagnose/deterministic` (f-meta-error-log-01). rationale-note ist vertraglich nicht-autoritativ („kein Mastery, kein Review-Fortschritt"; `archetypes.json`), choice-diagnose liefert autoritative Evidence — der Evidence-Vertrag der beiden Mitglieder ist nicht identisch; für w01-e11 kann keine Feedbackgrammatik die Familien-Hypothese („Ursache nur symptomatisch statt kausal begründet") je auswerten. Der Shard setzt konsequent `reviewEligible: false` nur für w01-e11 — die Familie trägt damit zwei verschiedene Review-Policies. | Council-Entscheid: entweder dokumentierte Ausnahme (rationale-note als bewusst nicht-grammatikfähiges Familienmitglied, da nur 2 Quellen dieser Archetypklasse im Korpus existieren) oder f-meta-error-log-01 → `classify-error-hypothesis` verschieben (dessen „Symptom statt Ursache"-Achse passt; expected 2→3 anpassen). Registry hat die Familie mit expected=2 vorgegeben — kein Implementer-Verschulden, aber klärungsbedürftig. |
| F4 | **medium** | w03-e1, f-collections-output-01, f-collection-step-trace-01 | Kompressionsregel §4: Splits/Merges nur nach den drei Tests; Fragmentierung im bereits über-Toleranz liegenden Gesamtsystem (129 Familien, 74 Singletons) | `trace-dict-state-update` (2 Mitglieder) neben `trace-collection-state` (1 Mitglied, Registry-expected=1) — 3 Collection-State-Quellen in 2 Familien. Die Differenzierung ist fachlich echt (Dict-Update-Semantik: Überschreiben/del/setdefault/get-Default vs. Listen-Aliasing Mutation/Kopie; die autorierten Regeln von w03-e1 brauchen die Update-Achse, die in keiner der beiden Registry-Familien liegt), aber ein Merge in **eine** Collection-State-Familie (3 Mitglieder, vereinigte Hypothesen: Mutation/Kopie, Index-/Slice-Grenzen, Dict-Update-Semantik) wäre die komprimiertere, gleich vertragskonforme Lösung gewesen. | Council: Merge `trace-dict-state-update` + `trace-collection-state` → `trace-collection-state` mit erweiterten errorHypotheses (3 Achsen), expected=3; alternativ die Trennung mit der obigen Begründung in die Registry aufnehmen. Kein Stillbruch im Shard. |
| F5 | **low** | f-testing-choice-01, f-meta-error-classify-01 | Kohärenz der Registry-Familiengrenzen (§5: Registry ist kanonisch; Review prüft fachliche Erwartung) | `classify-test-attitude` (f-testing-choice-01: „bester nächste Diagnoseschritt bei Off-by-one-Beobachtung") und `classify-error-hypothesis` (f-meta-error-classify-01: „welcher nächste Schritt des Debug-Prozesses passt zur Beobachtung") sind praktisch dieselbe Aufgabenform (Fehlerbild → bester nächster Debug-Schritt), liegen aber in verschiedenen Familien. Beide Zuordnungen folgen den Registry-hypoSources/expected-Werten — der Shard ist registry-konform, die Registry-Grenze selbst ist unscharf. | Council/D1: Grenze `classify-test-attitude` ↔ `classify-error-hypothesis` definieren (Test-Haltung/Attitüde vs. Ursachen-Hypothese) oder mergen; foundations nicht änderungspflichtig. |
| F6 | **low** | w01-e1, w01-e2, w02-e3, w03-e2 (Quellenvermerk) | Informationsverlust Quellsemantik → v2-Modell (Prüfpunkt Primary Claim/learningObjective; S4B-Relevanz) | Quellen marksieren „Diagnoseaufgaben zählen als Bearbeitungsnachweis, nicht als Mastery-Nachweis" (`content/exercises/w01.json` w01-e1/w01-e2, `w02.json` w02-e3, `w03.json` w03-e2). Das v2-Shard-Modell hat dafür kein Feld; `reviewEligible: true` bleibt stehen. Kein Schema-Bruch, aber die Mastery-vs-Bearbeitungsnachweis-Semantik darf in S4B-Binding/Placement nicht verloren gehen. | In der decision-queue bzw. S4B-Bindingspezifikation vermerken: Quelle-Mastery-Einschränkungen („Bearbeitungsnachweis") pro currentDefinitionId mitführen. |
| F7 | **low** (Info) | diverse (foundations-intern keine Fehlmitglieder) | Registry-Reconcile-Pflicht §5 („jede erwartete Anzahl gleich der abgeleiteten") | Assembler-Trockenlauf: foundations-bezogene Abweichungen `transform-expression-simplify-canonical` 7→3, `validate-required-field-raise` 13→5, `aggregate-accumulator-count` 12→4, `validate-test-design-coverage` 4→2, `construct-guarded-loop` 4→2, `classify-control-construct` 2→1; ferner 7 neue Familien ohne Registry-Eintrag (erwarteter Zwischenlauf mit `--allow-new-families`). Meine Kreuzprüfung: für keine dieser Lücken existiert eine foundations-Quelle, die falsch läge; die Übererfüllungen (`trace-assignment-state` 14→23, `aggregate-validate-and-count-records` 4→8) stammen aus anderen Shards. | Orchestrator/Council: Registry-Erwartungen nach den abgeleiteten Mitgliedschaften reconcilieren (§5); neue Familien formal beschließen. Keine Korrektur am foundations-Shard erforderlich. |

Keine critical-Findings. Kein Geheimnis-, IDs-, Persistenz- oder Sicherheitsverstoß.

## Abgearbeitete Prüfliste

1. **ID-Abdeckung & Zeichentreue:** 49/49 sourceIds mengengleich mit v1, keine Duplikate, keine Extra-/Fehl-IDs; `currentDefinitionId` 49/49 identisch mit v1; `legacyWeekId`/`sourceKind` korrekt (21 legacy-week-exercise, 28 canonical-definition). **Bestanden.**
2. **ruleIndex-Deckung:** Alle 45 v1-Feedbackvorkommen exakt je Quelle dispiert (0…n−1, keine Duplikate, keine Lücken). Verteilung: 31 consumed, 14 not-consumed — deckungsgleich mit v1 `consumerStatus` (31/14). **Bestanden.**
3. **Uncertainty-Dispositions (7/7):** field/level/reason zeichengetreu aus v1 (`w01-e3` proposedFamilyId/low, `w02-e1`,`w02-e2`,`w03-e1`,`w04-e1` feedbackConsumerStatus/medium, `w04-e2` proposedFamilyId/medium, `w04-e3` feedbackConsumerStatus/low). 2× resolved (w01-e3 `resolved-with-source-evidence`: gemeinsame Familie mit getrennten caseIds, wie v1-resolution gefordert; w04-e2 `resolved-with-code-evidence`: belegt — `BRANCH_SHAPES` in `foundations_fresh_generators.mjs` enthält elif-Formen `if-elif-elif-else`, `if-elif-elif-elif-else`), 5× `deferred-to-s4c` mit vollständigen owner/decision/evidence/latestGate. Kein `resolved-by-review-council`. **Bestanden.**
4. **Human-Review-Dispositions (11/11):** required + reasons zeichengetreu; 9× `deferred-to-s4c`, 2× `deferred-to-s4b` (w03-e1, f-collections-output-01); alle offenen mit owner/decision/evidence/latestGate; keine vorgetäuschte menschliche Freigabe. **Bestanden.**
5. **Fachliche Kohärenz je Familie (drei Tests):** Überwiegend überzeugend belegt (classify-git-operation: 4 Quellen über dasselbe Zustands-Konzeptsystem parametrisiert — GIT_CASES-Bank in `foundations_fresh_generators.mjs` bestätigt rein parametrische Variation; transform-linear-equation-isolate: Multi-Archetyp numeric+choice zulässig, da Lösungsweg [skalare Äquivalenzumformung] und Hypothesen [Vorzeichenfehler, einseitige Umformung] identisch — f-algebra-equivalence-01 bewertet genau diese Umformungen; aggregate-accumulator-count, validate-test-design-coverage, trace-exception-path, aggregate-validate-and-count-records [w03-e3 implementieren vs. f-data-code-repair-01 reparieren: gleiches Validierungs-/Zähl-Referenzmodell, Variation parametrisch über taskMode] kohärent). Ausnahmen: **F1 (w02-e1), F2 (w01-e6), F3 (reflect), F4 (Granularität), F5 (Grenze)**. **Teilweise beanstandet.**
6. **Zu breite/too enge Familien:** `trace-assignment-state` (7 foundations-Mitglieder, 3 Archetypen) ist die einzige Container-Verdachtsfläche: 5 der 7 Mitglieder sind saubere Zuweisungs-Traces (w01-e3, w01-e4, w01-e7, f-python-state-trace-01, f-code-reading-output-01 [Methodenkette = Zustandsfortschreibung über Unveränderlichkeit, `state-object-confusion`-Achse trägt]); w01-e6 (F2) und w02-e1 (F1) tragen nicht. Zu enge Familien: F4; die 5 construct/classify-Singletons sind begründet (s. Frage 4). **Beanstandet nur via F1/F2/F4.**
7. **Registry-Verträge byte-genau:** Für alle 18 Registry-Familien sind summary, solutionPath, referenceModel zeichengetreu und errorHypotheses mengengleich übernommen; familienintern identisch bei jedem Mitglied; familyGroup stimmt mit Registry überein. Neue 7 Familien haben vollständige, kohärente Verträge mit sinnvoller familyGroup (transform-terms ×1, trace-state ×1, classify-concept ×2, construct-program ×3); keine stillen Namensvarianten (Token-Multiset-Kollision gegen alle 64 Registry-IDs geprüft: keine). **Bestanden** (Reconciliation der neuen Familien steht aus, F7).
8. **TaskArchetype:** 49/49 korrekt aus activityType/graderId abgeleitet (numeric-exact 7, choice-diagnose 15, output-predict-lines 11, state-trace-vars 4, program-ordering 5, code-test 4, expression-equivalence 2, rationale-note 1); alle aktiven Archetypen, `graderAdapter` 49/49 identisch mit Archetyp-Adapter. Multi-Archetyp-Familien: 7; 6 davon vertretbar (identischer Lösungsweg + Hypothesen), 1 beanstandet (F3). **Bestanden mit Ausnahme F3.**
9. **Parametrische Variation & caseIds:** Alle 49 caseIds zeichengetreu = v1 `proposedCaseType`; keine Duplikate innerhalb Familie×Archetyp; keine Pseudo-Falltypen erfunden; Fallunterscheidungen (fix vs. seeded, shape-Banken, GIT_CASES, taskMode repair) sind als Fallparameter modelliert, nicht als programmatische Familienverzweigung. `authorityMode`: 13× seeded exakt deckungsgleich mit v1 `solverContract.kind=seed-generator`, 36× static — kanonische Abbildung §4 fehlerfrei (die sechs familyContractVariants-Fälle betrafen foundations-Quellen w01-e1/w01-e8 und f-algebra-both-sides-01: korrekt als static/static/seeded in einer Familie aufgelöst). **Bestanden.**
10. **Fehlerhypothesen & Feedback-Zielpfade:** Die 31 konsumierten Regeln sind allesamt Grammatik-fähige Prädikate (value===n, choice===x/!==x, value:<var>, element-count-mismatch, order-length-mismatch; gegen `assets/js/core/graders.js` verifiziert: diagnoseNumeric, gradeChoice, gradePredictOutput, gradeCodeTrace, parsonsDiagnosis vorhanden, Token vorhanden) → `grader-predicate` korrekt und verhaltensstabil. Kein not-consumed-Regel als grader-predicate (0 Fälle). 14 not-consumed: 11× answer-note (davon 4 code-test = checkId-Kanal exakt nach Vertrag; 7 predict/output-Fälle gedeckt durch den Archetyp-Vertrag „nimmt die unkonsumierten output-lines-Regeln auf" — Rationales deklarieren ehrlich „künftiger Diagnosekanal", kein Scheinkonsum), 1× worked-solution (w02-e2#0, korrekt), 1× generic-hint (f-algebra-both-sides-01 wrong-sign — vertretbar; alternative wäre das v2-sign-flip-Prädikat), 1× input-validation (f-algebra-final-boss-01 unparsed, korrekt). Deckung der errorHypotheses durch autorierte Regeln: verletzt nur bei w02-e1 (F1). **Bestanden mit Ausnahme F1.**
11. **Difficulty-Monotonie:** 49/49 = v1 `proposedDifficultyProfile`; innerhalb der Familien keine invertierten oder willkürlichen Profile (z. B. transform-linear-equation-isolate: basic→basic→advanced für beidseitiges Sammeln; trace-assignment-state: basic×4 → core×3; construct-guarded-loop: core→advanced für repair). **Bestanden.**
12. **Primary Claim vs. coEvidence:** primary 49/49 ∈ v1 competencyIds; coEvidence 20 Einträge, alle ⊆ v1 competencyIds; Stichproben gegen learningObjective konsistent (w01-e11 c-meta-learning, f-files-parsons-01 c-python-files-errors, w04-e3 c-testing-debugging u. a.). Anmerkung F6 zur verlorenen Bearbeitungsnachweis-Semantik. **Bestanden.**
13. **Persistenz & ID-Sicherheit:** 49/49 `preserve-id` (v1 persistenceCompatibility: alle preserve); keine merge-map-required/retire-blocked-Fälle in foundations (korrekt — die fünf Merge-Fälle liegen in anderen Domänen); keine abgeleitete ID wird Attempt-Schlüssel. **Bestanden.**
14. **Singleton-Begründungen:** 5 neue Singletons + 6 Registry-Singletons im Shard. Die 6 Registry-Singletons folgen hypoSources/expected der Registry (classify-test-attitude, classify-python-collection-choice, classify-exception-placement, classify-control-construct [Gesamt-Singleton 1/2, F7], trace-collection-state [1/1], validate-required-field-raise [1/13, Frage 2]). Die 5 neuen Singletons: berechtigt (Frage 4). Fragmentierungs-Anmerkung F4/F5. **Bestanden mit Anmerkungen.**
15. **Composite-Entscheidung:** 49/49 atomic-case; kein foundations-Quellobject ist ein echter Mehrteiler (alle Einzelübungen; der einzige Composite w05-e7 liegt korrekt in linear-algebra). **Bestanden.**

## Stellungnahme zu den sechs offenen Implementer-Fragen

1. **`transform-expression-simplify-canonical` … Erwartung 7 vs. 2 (+linalg 1):** **Die Erwartung zerfällt; es fehlen keine Mitglieder.** Beleg: Der Gesamtkorpus enthält exakt 3 Quellen mit `graderId: pyodide-sympy` (w01-e2, f-algebra-final-boss-01, w05-e5) — alle drei sind zugeordnet, und `expression-equivalence` sitzt damit exakt auf seinem expectedSourceCount 3. Die Registry hat für die Familie `hypothesisMemberSourceIds: []` und expected=7 — eine reine Design-Erwartung ohne Quellenbindung; vermutlich wurden Exponenten-Aufgaben (w01-e9/w01-e10) mitgedacht, die zu Recht in `transform-power-log-exponent` liegen (anderes Referenzmodell: Exponentenarithmetik fester Basis mit numerischer Antwort vs. SymPy-Äquivalenz gegen kanonische Zielform; in der Registry existiert keine Potenz/Log-Familie — verifiziert). foundations-Zuordnungen korrekt; Empfehlung: Council setzt expected 7→3.
2. **f-files-parsons-01 in `validate-required-field-raise`:** **Die drei Mitgliedschaftstests bestehen.** Prozedurtest: Die Quelle ist ausdrücklich „ein Altersfeld prüfen und einen erwarteten ValueError als Issue speichern" (`content/exercise-definitions/foundations/files-parsons.json`) — die Familien-Schablone „Pflichtfelder prüfen → deterministisch Fehler auslösen → Verletzungsliste erzeugen" führt durch die Parsons-Sequenz (try/parse_age/except ValueError/issues.append); das Anordnen ist Archetyp-, nicht Familieigenschaft. Referenzmodelltest: geordnete Referenzsequenz als Parsons-Autorität — Multi-Archetyp neben den code-test-Mitgliedern (w26-e5, w31-e5, w32-e4, w38-e4) zulässig, da Lösungsweg und Hypothesen identisch bleiben. Diagnosetest: Der Distraktor `except Exception: pass` = Hypothese „Fehlertyp oder Auslösezeitpunkt falsch" — exakt abgedeckt. Die 13er-Erwartung zerfällt allerdings real (5 gesamt; Implementer-Angabe „dml/dl 0" stimmt, tf+cap tragen 4) → F7. Keine Korrektur am foundations-Shard.
3. **Neue Familie `trace-dict-state-update`:** **Vertretbar, mit Merge-Empfehlung (F4).** Gegen `trace-assignment-state` spricht der Diagnosetest: Dessen Hypothesen (reads-new-value, overwrite-forgotten, print-order, state-object-confusion) enthalten keine Dict-Update-Semantik-Achse (Überschreiben/del/setdefault/get-Standard) — die autorierten w03-e1-Regeln brauchen sie. Gegen `trace-collection-state` spricht dessen Aliasing-Fokus (Mutation/Kopie), der die setdefault/del-Diagnosen ebenfalls nicht trägt. Die neue Familie löst das korrekt; alternativ hatte der Implementer `aggregate-accumulator-count` für f-collections-output-01 richtig verworfen (die „2 2"-Antwort ist Endzustands-Lesung von Werten und Schlüsselmenge, keine reine Zählaufgabe; die neue Hypothese „Aggregation über das falsche Dict-Objekt" adressiert genau die Werte-vs-Schlüssel-Verwechslung). Komprimierender wäre ein Merge mit `trace-collection-state` gewesen — Council-Entscheid (F4).
4. **Die fünf neuen Singletons:** **Berechtigt; keine gehören als CaseTemplate in eine bestehende Familie.** `construct-regression-test-suite` (w04-e3): Lösungsweg ist Normalisierung implementieren **plus** Testsuite autoren — weder `validate-test-design-coverage` (bewertet gegebene Testmengen) noch `construct-guarded-loop` (Schleifenkonstruktion) teilen diesen Weg. `construct-test-structure-aaa` (f-testing-parsons-01): AAA-Strukturvertrag mit erwartungsunabhängigem Assert (Distraktor „expected = issues" = exakt die Familien-Hypothese) — Abdeckung ist ein anderes Kriterium als Struktur. `construct-safe-bugfix-workflow` (f-git-parsons-01): Workflow-Ordnung mit Testvertrag-Distraktor („fehlschlagenden Test löschen") — kein Code-Konstruktions- und kein Git-Konzeptsystem-Klassifikationsweg. `classify-string-immutability` (w02-e3) und `classify-set-operation-semantics` (w03-e2): Klassifikations-Singletons mit eigenem Konzeptsystem (Immutabilität: erzeugende vs. ändernde Operation mit deterministischem TypeError; Mengensemantik: Literal-Dedup, Durchschnitt vs. Vereinigung, len über Eindeutige) — legitim nach §4, begründungspflichtig erfüllt durch vollständige Verträge; `classify-python-collection-choice` (Strukturwahl) ist ein anderes Konzeptsystem, kein Merge-Ziel. Einzige Anmerkung: Set-Semantik als „Collection-Konzept" wäre oberflächlich mergefähig mit der Strukturwahl — die Differenz (Wahl einer Struktur für einen Zweck vs. Vorhersage der Wirkung einer Operation) ist aber echt; mit F4 zusammen bringt foundations 3 classify-Familien im Collection-Umfeld, was der Council im Blick behalten sollte.
5. **w02-e2#0 als worked-solution:** **Zustimmung.** Die Nachricht durchläuft die Akkumulatorschritte („bei i=0 entsteht ‚p', bei i=3 dann ‚tp'") — das ist die Lösungsschrittfolge der Präfixkette selbst, kein Diagnoseprädikat auf eine beobachtbare Fehlausgabe. Konsequent bleibt #1 (Schrittweiten-Diagnose „range(0, 9, 3)") als answer-note. Die Grenze ist damit sauber und beispielgebend.
6. **`reflect-error-journal-rationale` 2/2 voll:** **Faktisch bestätigt, formal beanstandet (F3).** Verifiziert: w05-e9 liegt im linear-algebra-Shard bei `formula-scalar-product` (Archetyp rationale-note, caseId column-picture-self-assessment); rationale-note sitzt damit gesamt exakt 2× (expectedSourceCount erfüllt); reflect-error-journal-rationale hat keine Erwartungslücke. Die Familienbildung selbst trägt jedoch einen nicht-identischen Evidence-Vertrag (nicht-autoritatives manual-rubric neben autoritativem choice; für w01-e11 existiert keine Grammatik für die Hypothese) — dies war vom Implementer korrekt als offener Punkt gemeldet und braucht den Council-Beschluss aus F3.

## Gesamturteil

**Coral (nachbesserungsbedürftig, kein Blocker).**

Begründung: Die Maschinen-verifizierbare Struktur ist vollständig in Ordnung — 49/49 IDs, exakte ruleIndex-Deckung, zeichentreue uncertainty-/humanReview-Übernahmen, byte-genaue Registry-Verträge, korrektes Archetyp-/Adapter-/authorityMode-/difficulty-/caseId-Mapping, saubere Persistenz, ehrliche Feedback-Zielpfade ohne Scheinkonsum. Die sechs Implementer-Fragen sind durchweg datengestützt und überwiegend richtig entschieden. Verhindert eine grüne Freigabe: **F1** — w02-e1 verletzt den Diagnosetest in `trace-assignment-state` (die Slice-Grenzen-Diagnosen der Quelle liegen außerhalb jeder Familien-Hypothese und sind in der Registry einer anderen Familie zugeordnet); dazu **F2** (w01-e6 ohne jede Zuweisung in der Zuweisungs-Trace-Familie bei invertierter Grenze zu w01-e5) und **F3** (Multi-Archetyp mit nicht-identischem Evidence-Vertrag in reflect-error-journal-rationale). F1 und F2 erfordern Council-/Registry-Entscheidungen (Zielfamilie bzw. expected-Anpassung), kein bloßes Shard-Patchen; F3 verlangt eine dokumentierte Ausnahme oder eine Verschiebung. Ohne Klärung dieser drei Punkte würde die Feedbackgrammatik der größten foundations-Familie (23 Mitglieder gesamt) zwei Mitglieder tragen, deren authentische Fehlerquellen sie nicht diagnostizieren kann — genau der Mischmasch aus Themenähnlichkeit, den der Modellvertrag ausschließen will. F4/F5/F6/F7 sind Verbesserungs- bzw. Integrationsnotizen ohne Korrekturpflicht am Shard.

**Integrationarbeit für den Parent/Council (Block C):** Registry-Reconcile der foundations-beeinflussten Erwartungen (F7: 7→3, 13→5, 12→4, 4→2 ×2, 2→1, 14→23, 4→8), Beschluss über die 7 neuen Familien, Entscheidung F1 (w02-e1-Zielfamilie), F2 (w01-e6), F3 (reflect-Ausnahme oder Verschiebung), F4 (Collection-State-Merge), F5 (classify-Grenze), S4B-Notiz F6.
