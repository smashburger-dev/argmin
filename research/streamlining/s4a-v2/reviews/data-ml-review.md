# Domänenreview data-ml (S4A-v2)

- **Shard:** `research/streamlining/s4a-v2/shards/data-ml.json` (61 Entries, 34 Familien: 18 Registry + 16 neue)
- **Datum:** 2026-09-03
- **Reviewer:** unabhängiger Domänenreviewer (nicht der Implementer), adversarial
- **Prüfungsumfang:** `family-model.md` (Stand 2026-09-02), `taxonomy.schema.json` (überflogen), `archetypes.json`, `canonical-families.json`, `inputs/data-ml-v1.json`, Shard vollständig; Quellbelege aus `content/exercises/w06.json`–`w17.json`, `assets/js/core/graders.js`, `assets/js/core/data_ml_generators.mjs` (Stichproben), sämtlichen anderen Shards (lesend, nur Mitgliedschaftszählung).

## Verifikationsläufe (eigene Skriptauswertungen)

- `node research/streamlining/s4a-v2/scaffold-shard.mjs --validate data-ml` → **bestanden**: „Eintraege 61/61, ID-Mengen exakt, Schema valide, TODO-Platzhalter 0", Exit 0.
- `node research/streamlining/s4a-v2/selftest.mjs` → **27/27 bestanden**, Exit 0.
- `node research/streamlining/s4a-v2/assemble-v2.mjs` (Trockenlauf) → meldet 94 Registry-Verletzungen (Liste zeigt nur 20, Rest abgeschnitten). Data-ml-relevant per eigener Nachzählung über alle sieben Shards: 8 Registry-Familien mit Count-Abweichung, 16 neue data-ml-Familien ohne Registry-Eintrag (Details F3).
- Eigene `node -e`-Auswertungen (`/tmp/dml_check.mjs`, `/tmp/dml_check2.mjs`, `/tmp/dml_asm.mjs`) gegen v1/Shard/Registry/Archetypen/Kompetenzen: ID-Mengen, ruleIndex-Deckung, consumerStatus-Mapping, uncertainty-/humanReview-Zeichentreue, byte-genaue Registry-Verträge, authorityMode-Abbildung, Archetyp-/Adapter-Mapping, difficulty, competencyClaim, placement-lessonIds, caseId-Bindungen (Ergebnisse in Prüfliste).

## Findings

| ID | Schweregrad | sourceIds | verletzte Regel | Beleg (Datei + Fundstelle) | kleinste Korrektur |
|---|---|---|---|---|---|
| F1 | **medium** | w13-e4 | Prozedurtest + Referenzmodelltest (family-model §4 Tests 1–2); Grenzfall-Dokupflicht §10.5 | Shard `shards/data-ml.json`, Eintrag w13-e4: solutionPath „Zählobjekt {tp, fp, fn, tn} bilden bzw. ablesen, den Metrikselektor anwenden und die geschlossene Formel mit Nullteilerschutz auswerten", referenceModel „Zählobjekt {tp, fp, fn, tn} plus Metrikselektor". Quelle `content/exercises/w13.json`, `w13-e4`: `subgroup_error_rates(y_true, y_pred, groups)` baut `{Gruppe: Fehler/Anzahl}`, `largest_gap` bildet Maximum minus Minimum; kein tp/fp/fn/tn-Objekt, keine Metrikselektion, keine geschlossene Formel mit Nullteilerschutz. Der Metrikselektor-Schritt der Familien-Schablone läuft bei w13-e4 leer. Die Nachbarfamilie `aggregate-grouped-metrics-report` (Shard-Einträge w07-e5, w13-e5) beschreibt w13-e4 exakt: „Gruppen ableiten, je Gruppe Kennzahlen (Fehlerraten) berechnen, Anteile und Extremwerte ableiten" plus Referenz „versteckte deterministische Testsuite mit eingebetteter Referenzimplementierung". Auch der Diagnosetest spricht für dort: w13-e4-typische Fehler („Gruppen als eigene Schleifen statt Zähler-dict", „Längenprüfung weglassen") liegen auf den Gruppen-Hypothesen („gruppenweise statt über alle Zeilen", „Eingabevalidierung weggelassen"), in der Confusion-Familie trägt nur „wrong-denominator" einen von drei Fehlern. Die Entscheidung ist im Shard nirgends dokumentiert: w13-e4 hat keine proposedFamilyId-Uncertainty (v1 schlug die eigene Familie `data-ml-subgroup-error-rates` vor). | Council-Entscheid nach §10.5; fachlich sauber: w13-e4 → `aggregate-grouped-metrics-report` verschieben (abgeleitet confusion 15→14, grouped 3→4). Alternativ die Confusion-Zuordnung mit durchgeführtem Prozedurtest begründen und in die Registry aufnehmen. |
| F2 | **low** | w06-e1, w07-e1, w08-e1, w14-e1, w15-e1, w16-e1, w17-e1 | Primary Claim / S4B-Bindung (Prüfpunkt wie foundations-F6; kein Schema-Bruch) | Quellen `content/exercises/w06.json` (w06-e1), `w07.json` (w07-e1), `w08.json` (w08-e1) u. a.: fullSolution „Konzeptfrage: zählt als Bearbeitungsnachweis, nicht als Mastery-Nachweis." Shard: alle sieben mit `placement.reviewEligible: true`; das v2-Modell hat kein Feld für die Mastery-Einschränkung. | In decision-queue bzw. S4B-Bindung vermerken: Mastery-Einschränkung („Bearbeitungsnachweis") pro currentDefinitionId mitführen. Keine Shard-Änderung nötig. |
| F3 | **low** (Info, Empfänger Council) | diverse, keine Fehlzuordnung im Shard | Registry-Reconcile-Pflicht §5 („jede erwartete Anzahl gleich der abgeleiteten"); Block-D1-Materie, dem Shard nicht anzulasten | Assembler-Trockenlauf + eigene Nachzählung: `aggregate-confusion-metric` erwartet 20 / abgeleitet 15 (dml-Anteil 5); `formula-ratio-percent-metric` 13/7 (dml 5 + transformer-llm:w26-e4 + genai-systems:w30-e2); `optimize-gradient-update-rule` 14/4 (dml 2 + transformer-llm:w25-e5, w25-e6); `fit-predict-metrics` 9/3; `reproduce-seeded-split` 11/2; `classify-error-drift` 2/1; `classify-regularizer-effect` 2/1; `classify-fairness-aggregation` 1/2 (plus research-capstone:w33-e1); ferner 16 neue data-ml-Familien ohne Registry-Eintrag; `trace-assignment-state` 14→23 (dml-Anteil w08-e3, w15-e3, w17-e3). | Orchestrator/Council: Registry-Erwartungen nach den abgeleiteten Mitgliedschaften reconcilieren; 16 neue Familien formal beschließen. Keine Korrektur am data-ml-Shard erforderlich. |

Keine critical-, keine high-Findings. Kein IDs-, Persistenz- oder Sicherheitsverstoß.

## Abgearbeitete Prüfliste

1. **ID-Abdeckung & Zeichentreue:** 61/61 sourceIds mengengleich mit v1, keine Duplikate, keine Extra-/Fehl-IDs; `currentDefinitionId`, `legacyWeekId`, `sourceKind` 61/61 identisch mit v1. **Bestanden.**
2. **ruleIndex-Deckung:** Alle 170 v1-Feedbackvorkommen exakt je Quelle dispiert (0…n−1, keine Duplikate, keine Lücken). Verteilung: 83 consumed, 87 not-consumed. **Bestanden.**
3. **Fehlerhypothesen & Feedback-Zielpfade (kein Scheinkonsum):** 83× consumed → `grader-predicate`, 0× not-consumed → `grader-predicate`. 85× not-consumed → `answer-note` (davon 24 code-test-Fälle über den checkId-Kanal aus dem Archetyp-Vertrag; Stichprobe w06-e4 verifiziert: `__check`-Namen „fehlende Spalte -> ValueError", „bool -> ValueError", „duplicate_ids distinct" in `content/exercises/w06.json` tragen exakt die drei Rationales), 1× `generic-hint` (w14-e2#1 `value < 0`: kein exakter value-String erreichbar, v1 consumerStatus not-consumed — korrekt), 1× `editorial-diagnosis` (w14-e4#2 used-inv: Tests verlangen solve nicht — ehrlich dokumentiert). Grader-Prädikate gegen `assets/js/core/graders.js` verifiziert: `diagnoseNumeric` (Z. 54), `gradeChoice` (Z. 79), `gradeCodeTrace` mit `value:<var>`-Grammatik (Z. 391–419), `gradePredictOutput` mit `element-count-mismatch` (Z. 439–454). **Bestanden.**
4. **Uncertainty-Dispositions (64 Vorkommen):** field/level/reason 61/61 zeichengetreu aus v1, Count je Quelle gleich; 32× `resolved-with-source-evidence` (alle mit rationale; Stichproben w06-e1, w07-e2, w12-e2 substanziell, w07-e2 grenzt die Confusion-Familie ausdrücklich ab: „Zählobjekt ist keine Konfusionsmatrix"), 30× `deferred-to-s4c` mit vollständigen owner/decision/evidence/latestGate, 2× `requires-noa-decision` (Merge-Paar, s. Stellungnahme). Kein `resolved-by-review-council`. **Bestanden.**
5. **Human-Review-Dispositions:** v1 `humanReviewRequired` exakt übernommen (2× true: w12-e2, w17-e2, beide `requires-noa-decision` mit owner Noa, decision, evidence, latestGate S4D3; 59× false ohne Disposition). Reasons zeichengetreu. Keine vorgetäuschte menschliche Freigabe. **Bestanden.**
6. **Fachliche Kohärenz je Familie (drei Tests):** 33 von 34 Familien überzeugend. Ratio-Familie (5 Mitglieder, alle `numeric-exact`/seeded, identischer Zähler-Nenner-Weg; Quellen w07-e2, w10-e3, w13-e2, w14-e2, w16-e2 verifiziert: keine baut ein Confusion-Zählobjekt), Spread-Familie (w12-e2/w17-e2: identischer Generator genCvSpread, identischer Weg), Gradienten-Paar, MSE-Paar (numeric+code-test, geschlossenes (2/n)-Referenzmodell), Quadratic-Paar (choice+numeric, MSE/RMSE-Formel), CV-Leakage-Paar, Ridge/Lasso-Paar, Majority-Paar, Seeded-Split-Paar, Fit-Predict-Trio, PCA-Pipeline-Paar, Repro-Report-Paar, Cleaning-Paar, Library-Output-Trio, Quality-Contract-Paar, Grouped-Paar, alle 16 Singletons mit eigenem Konzept- bzw. Verfahrenssystem. Ausnahme: **F1 (w13-e4)**. Geprüfte und verworfene Alternativen: w08-e3 in `optimize-gradient-update-rule` statt `trace-assignment-state` (Proposal war `data-ml-gradient-loop-trace`) scheitert am Prozedurtest — Gradienten sind gegeben, Residuenbildung entfällt; der Zuweisungs-Trace trägt. w15-e3/w17-e3 folgen den Registry-hypoSources. **Teilweise beanstandet (F1).**
7. **Zu breite/zu enge Familien:** `aggregate-confusion-metric` (15 abgeleitet, 3 Archetypen) ist die Container-Verdachtsfläche: w11-e2 (Zählung), w11-e3 (Kosten-Schwellenwahl, choice, autoritative Evidence — Multi-Archetyp zulässig), w11-e4 (baut wörtlich `{'tp','fp','fn','tn'}` + precision_recall_f1 mit Nullteilerschutz), w11-e5 (Kostenminimierung darüber) kohärent; nur w13-e4 trägt nicht (F1). Zu enge Familien: keine ungerechtfertigte Fragmentierung gefunden; die 7 neuen Singletons (parameter-origin, leakage-audit, svm-margin, supervision-scaling, tree-split, ensemble-comparison, descriptive-stats) haben je eigenes Konzept-/Verfahrenssystem mit vollständigem Vertrag. **Beanstandet nur via F1.**
8. **Grenzziehung `formula-ratio-percent-metric` gegen `aggregate-confusion-metric`:** Für alle fünf data-ml-Ratio-Mitglieder einzeln entschieden (Lösungsweg, Zählobjekt, Fehlerhypothesen): w13-e2 rechnet Raten aus Rohzählungen ohne Confusion-Objekt (Ratio korrekt); w14-e2/w16-e2/w10-e3 reine Verhältnisformeln (Ratio korrekt); w07-e2 mit dokumentierter Abgrenzung im Shard. Confusion-seitig korrekt bis auf w13-e4 (F1). Die in §10.5 namentlich genannten Quellen (w26-e4, w33-e2, w34-e2) liegen in anderen Domänen (transformer-llm, research-capstone) und sind dort zu entscheiden. **Bestanden mit Ausnahme F1.**
9. **Registry-Verträge byte-genau:** Für alle 18 verwendeten Registry-Familien summary, solutionPath, referenceModel zeichengetreu und errorHypotheses mengengleich; familyGroup stimmt. Keine stillen Namensvarianten. **Bestanden** (Reconcile steht aus, F3).
10. **TaskArchetype:** 61/61 korrekt aus activityType/graderId (choice-diagnose 17, numeric-exact 14, output-predict-lines 5, code-test 24, state-trace-vars 1); `graderAdapter` 61/61 konsistent mit Archetyp-Adapter. 5 Multi-Archetyp-Familien, alle mit identischem Lösungs- und Evidence-Vertrag (choice/numeric/code-test liefern je autoritative Evidence; static/seeded-Mischung ist nach §4 ausdrücklich keine Grenze). **Bestanden.**
11. **Parametrische Variation, caseIds, authorityMode:** 61 distinkte caseIds, Bindung caseId→Familie/Archetyp/Adapter konsistent, keine Duplikate in Familie×Archetyp. `authorityMode` 14× seeded exakt deckungsgleich mit v1 `solverContract.kind=seed-generator` (w06-e2, w06-e6, w07-e2, w08-e2, w09-e2, w10-e2, w10-e3, w11-e2, w12-e2, w13-e2, w14-e2, w15-e2, w16-e2, w17-e2), 47× static — kanonische Abbildung §4 fehlerfrei. **Bestanden.**
12. **Difficulty-Monotonie:** 61/61 = v1 `proposedDifficultyProfile`; familieninterne Staffelungen plausibel (z. B. confusion basic→core→advanced→final-boss; quality-contract advanced→final-boss; quadratic basic→core). **Bestanden.**
13. **Primary Claim vs. coEvidence:** primary 61/61 ∈ v1 competencyIds, coEvidence ⊆ v1 competencyIds, alle IDs existieren in `content/competencies/core.json` (0 Verletzungen); lessonIds 61/61 = v1. Anmerkung F2. **Bestanden.**
14. **Persistenz & ID-Sicherheit:** 60× `preserve-id`, 1× `merge-map-required` (w17-e2, `mergeInto: formula-metric-spread-range` — nicht-leer, Konvention wie alle anderen Merge-Fälle shardübergreifend, die die Zielfamilie nennen). Kein stiller Merge, keine abgeleitete ID als Attempt-Schlüssel, kein Composite (61/61 atomic-case; keine Quelle ist ein echter Mehrteiler). Zum Merge-Zielpfad s. Stellungnahme. **Bestanden.**
15. **Singleton-Begründungen:** 16 data-ml-lokale Singletons; 5 folgen Registry-Erwartung =1 (missingness, confounding, task-type, sigmoid-regime, ensemble-effect, repro-contract — sechs, alle hypo-gedeckt), classify-fairness-aggregation hat zweiten Member in research-capstone (F3), die 7 neuen Singletons tragen je eine schriftliche Begründung in der Uncertainty-Rationale (eigenes Konzeptsystem, kein Registry-Eintrag). **Bestanden mit Anmerkung F3.**

## Stellungnahme zu den offenen Implementer-Fragen

Der Shard enthält kein Fragenfeld; die einzigen offenen Entscheidungen sind die zwei gekoppelten Noa-Dispositionen zum w17-e2-Merge:

1. **w17-e2 → `formula-metric-spread-range` (merge-after-extraction):** **Zustimmung mit Auflage.** Beleglage: identischer Generator genCvSpread (`assets/js/core/data_ml_generators.mjs`), identischer Spannweiten-Lösungsweg, v1-`targetRationale` „merge-after-extraction" — der Familienvertrag hält das Repro-Framing als eigenen Falltyp (`repro-report-fold-spread` neben `fold-stability-spread`) fest, Difficulty beidseits core-application. Die Kehrseite ist verifiziert und korrekt behandelt: w17-e2 trägt primary `c-ml-repro`, die Ziel-Primärinstanz w12-e2 nur `c-ml-cv`; der Shard trägt die Doppelkompetenz-Erhaltung ausdrücklich als Freigabebedingung in decision/evidence (latestGate S4D3) ein, statt sie still zu verletzen oder w12-e2 mit einer nicht in v1 gedeckten coEvidence zu belegen (w12-e2 v1-competencyIds = [c-ml-cv] allein — eine Vorab-Ergänzung wäre ein Subset-Bruch). Beide Seiten verweisen aufeinander. Auflage an S4B/S4D: beim Merge die `c-ml-repro`-Evidenz auf die Zielinstanz migrieren, sonst schrumpft die Mastery-Evidenz der Woche w17.
2. **w12-e2 als Primärinstanz:** **Zustimmung.** Einzige saubere Ankerinstanz (identischer Vertrag, früherer Fold-Kontext `l-ml-cv`); Disposition spiegelt die Merge-Bedingung.

## Gesamturteil

**Coral (nachbesserungsbedürftig, kein Blocker).**

Begründung: Die maschinen-verifizierbare Struktur ist vollständig in Ordnung — 61/61 IDs, exakte ruleIndex-Deckung über 170 Vorkommen, zeichentreue uncertainty-/humanReview-Übernahmen, byte-genaue Registry-Verträge, korrektes Archetyp-/Adapter-/authorityMode-/difficulty-/Claim-/lesson-Mapping, saubere Persistenz (w17-e2 mit nicht-leerem mergeInto und beidseitig dokumentierter Noa-Entscheidung), ehrliche Feedback-Zielpfade ohne einen einzigen Scheinkonsum-Fall. Verhindert eine grüne Freigabe: **F1** — w13-e4 besteht Prozedur- und Referenzmodelltest der Confusion-Familie nicht (kein {tp,fp,fn,tn}-Zählobjekt, kein Metrikselektor, Referenzimplementierung statt geschlossener Formel) und passt in allen drei Tests besser nach `aggregate-grouped-metrics-report`; die Entscheidung ist im Shard undokumentiert. Ohne Council-Klärung trüge die größte cross-domain-Familie (15 Mitglieder) ein Mitglied, dessen authentische Fehlerquellen ihre Grammatik nur zu einem Drittel diagnostiziert — genau die Themenähnlichkeits-Falle, die §4 ausschließen will. F2 ist eine S4B-Notiz, F3 reine Council-Materie.

**Integrationarbeit für den Parent/Council (Block C/D1):** Registry-Reconcile der acht abweichenden Erwartungen + Beschluss über 16 neue Familien (F3); Entscheid F1 (w13-e4-Zielfamilie); S4B-Notiz F2; Merge-Freigabe w17-e2/w12-e2 mit Claim-Migration `c-ml-repro` (Stellungnahme 1).
