{
  "schemaVersion": 1,
  "familyId": "validate-data-quality-contract",
  "contract": {
    "familyId": "validate-data-quality-contract",
    "familyGroup": "validate-contract",
    "summary": "Implementiert Datenqualitätsverträge mit Schema-, Typ-, Bereichs- und Duplikatprüfungen.",
    "taskArchetype": "code-tests",
    "authorityMode": "static",
    "masteryEligible": true,
    "caseTypes": [
      {
        "caseId": "profile-table-schema-counts",
        "propertyTest": false
      },
      {
        "caseId": "validate-rows-contract-errors",
        "propertyTest": false
      }
    ],
    "difficultyProfiles": [
      "core",
      "stretch"
    ],
    "competencyIds": [
      "c-pandas-cleaning"
    ],
    "graderId": "pyodide",
    "activityType": "python-code"
  },
  "cases": [
    {
      "caseId": "profile-table-schema-counts",
      "difficultyProfile": "core",
      "masteryEligible": true,
      "sourceLineage": [
        "w06-e4"
      ],
      "parameters": {
        "packages": [
          "numpy"
        ],
        "starterCode": "def profile_table(rows):\n    \"\"\"Return {'total', 'missing', 'sentinel', 'duplicate_ids'} or raise ValueError.\"\"\"\n    # check schema per row first, then count\n    ...\n",
        "tests": "rows = [\n    {\"id\": \"a\", \"wert\": 3},\n    {\"id\": \"b\", \"wert\": None},\n    {\"id\": \"a\", \"wert\": -1},\n    {\"id\": \"c\", \"wert\": -1},\n    {\"id\": \"d\", \"wert\": 7},\n    {\"id\": \"b\", \"wert\": 0},\n]\np = profile_table(rows)\n__check('total', p[\"total\"] == 6)\n__check('missing', p[\"missing\"] == 1)\n__check('sentinel', p[\"sentinel\"] == 2)\n__check('duplicate_ids distinct', p[\"duplicate_ids\"] == 2)\ntry:\n    profile_table([{\"id\": \"a\", \"wert\": 3}, {\"id\": \"b\"}])\n    __check('fehlende Spalte -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('fehlende Spalte -> ValueError', True)\ntry:\n    profile_table([{\"id\": \"a\", \"wert\": \"3\"}])\n    __check('falscher Typ -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('falscher Typ -> ValueError', True)\ntry:\n    profile_table([{\"id\": \"a\", \"wert\": True}])\n    __check('bool -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('bool -> ValueError', True)"
      },
      "expected": {
        "kind": "reference-solver",
        "referenceSolver": "def profile_table(rows):\n    REQUIRED = {\"id\", \"wert\"}\n    total = 0\n    missing = 0\n    sentinel = 0\n    counts = {}\n    for row in rows:\n        if not isinstance(row, dict) or set(row.keys()) != REQUIRED:\n            raise ValueError(\"schema violation: expected columns id, wert\")\n        rid, wert = row[\"id\"], row[\"wert\"]\n        if not isinstance(rid, str):\n            raise ValueError(\"schema violation: id must be str\")\n        if wert is not None and (not isinstance(wert, int) or isinstance(wert, bool)):\n            raise ValueError(\"schema violation: wert must be int or None\")\n        total += 1\n        if wert is None:\n            missing += 1\n        elif wert == -1:\n            sentinel += 1\n        counts[rid] = counts.get(rid, 0) + 1\n    duplicate_ids = sum(1 for c in counts.values() if c > 1)\n    return {\"total\": total, \"missing\": missing, \"sentinel\": sentinel, \"duplicate_ids\": duplicate_ids}"
      },
      "prompt": "Implementiere <code>profile_table(rows)</code>. Eingabe: <code>rows</code> ist eine Liste von Dictionaries mit genau den Spalten <code>\"id\"</code> (immer str) und <code>\"wert\"</code> (int oder None). Rückgabe: ein Dictionary mit <code>\"total\"</code> (Anzahl der Zeilen), <code>\"missing\"</code> (Anzahl Zeilen mit wert None), <code>\"sentinel\"</code> (Anzahl Zeilen mit wert -1) und <code>\"duplicate_ids\"</code> (Anzahl verschiedener ids, die in mehr als einer Zeile vorkommen). Schema-Verstöße führen zu <code>ValueError</code>, und zwar bevor irgendein Ergebnis zurückgegeben wird: falsche oder fehlende Spalten, id kein str, wert weder int noch None; <code>bool</code> gilt nicht als int. Der Testcode ist von deiner Eingabe getrennt und prüft Werte und Fehlerverhalten.",
      "fullSolution": "def profile_table(rows):\n    REQUIRED = {\"id\", \"wert\"}\n    total = 0\n    missing = 0\n    sentinel = 0\n    counts = {}\n    for row in rows:\n        if not isinstance(row, dict) or set(row.keys()) != REQUIRED:\n            raise ValueError(\"schema violation: expected columns id, wert\")\n        rid, wert = row[\"id\"], row[\"wert\"]\n        if not isinstance(rid, str):\n            raise ValueError(\"schema violation: id must be str\")\n        if wert is not None and (not isinstance(wert, int) or isinstance(wert, bool)):\n            raise ValueError(\"schema violation: wert must be int or None\")\n        total += 1\n        if wert is None:\n            missing += 1\n        elif wert == -1:\n            sentinel += 1\n        counts[rid] = counts.get(rid, 0) + 1\n    duplicate_ids = sum(1 for c in counts.values() if c > 1)\n    return {\"total\": total, \"missing\": missing, \"sentinel\": sentinel, \"duplicate_ids\": duplicate_ids}\n\n# profile_table aus dem Test: total 6, missing 1, sentinel 2, duplicate_ids 2",
      "hints": [
        "Prüfe pro Zeile zuerst das Spaltenset und die Typen — ValueError vor dem Zählen. bool ist eine Unterklasse von int und muss explizit ausgeschlossen werden.",
        "Zähle fehlende und Sentinel-Werte in einem Durchlauf; für duplicate_ids sammle id-Häufigkeiten in einem Dictionary und zähle danach alle ids mit Häufigkeit > 1."
      ],
      "feedbackRules": [
        {
          "if": "valueerror-missing",
          "then": "Der ValueError bei Schema-Verstoß fehlt oder wird erst nach dem Zählen geworfen."
        },
        {
          "if": "bool-accepted",
          "then": "True/False sind Instanzen von int (Unterklasse) — isinstance(wert, bool) muss gesondert abgelehnt werden."
        },
        {
          "if": "duplicate-rows-vs-ids",
          "then": "duplicate_ids zählt verschiedene ids mit mehr als einer Zeile, nicht die Anzahl zusätzlicher Zeilen."
        }
      ],
      "typicalErrors": [
        "Schema-Prüfung nach dem Zählen ausgeführt oder weggelassen",
        "bool versehentlich als int akzeptiert",
        "Sentinel -1 nicht von fehlendem None unterschieden"
      ],
      "tolerancePolicy": {
        "mode": "tests"
      },
      "activityType": "python-code",
      "graderId": "pyodide",
      "variants": [
        {
          "parameters": {
            "tests": "rows = [{'id': 'x', 'wert': None}, {'id': 'y', 'wert': 4}, {'id': 'x', 'wert': -1}, {'id': 'z', 'wert': 8}]\np = profile_table(rows)\nids = [row['id'] for row in rows]\nwerte = [row['wert'] for row in rows]\ncounts = {}\nfor rid in ids:\n    counts[rid] = counts.get(rid, 0) + 1\n__want = {\n    'total': len(rows),\n    'missing': sum(1 for w in werte if w is None),\n    'sentinel': sum(1 for w in werte if w == -1),\n    'duplicate_ids': sum(1 for c in counts.values() if c > 1),\n}\n__check('total', p['total'] == __want['total'])\n__check('missing', p['missing'] == __want['missing'])\n__check('sentinel', p['sentinel'] == __want['sentinel'])\n__check('duplicate_ids distinct', p['duplicate_ids'] == __want['duplicate_ids'])\ntry:\n    profile_table([{'id': 'a', 'wert': 3}, {'id': 'b'}])\n    __check('fehlende Spalte -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('fehlende Spalte -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': '3'}])\n    __check('falscher Typ -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('falscher Typ -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': True}])\n    __check('bool -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('bool -> ValueError', True)\n\n"
          },
          "prompt": "Profiltabelle ids x/y/z, eine Lücke und ein Sentinel -1. Implementiere `profile_table` wie im Vertrag (total/missing/sentinel/duplicate_ids, ValueError bei Schema)."
        },
        {
          "parameters": {
            "tests": "rows = [{'id': 'p', 'wert': 0}, {'id': 'q', 'wert': -1}, {'id': 'r', 'wert': None}, {'id': 'p', 'wert': 2}, {'id': 'q', 'wert': 9}]\np = profile_table(rows)\nids = [row['id'] for row in rows]\nwerte = [row['wert'] for row in rows]\ncounts = {}\nfor rid in ids:\n    counts[rid] = counts.get(rid, 0) + 1\n__want = {\n    'total': len(rows),\n    'missing': sum(1 for w in werte if w is None),\n    'sentinel': sum(1 for w in werte if w == -1),\n    'duplicate_ids': sum(1 for c in counts.values() if c > 1),\n}\n__check('total', p['total'] == __want['total'])\n__check('missing', p['missing'] == __want['missing'])\n__check('sentinel', p['sentinel'] == __want['sentinel'])\n__check('duplicate_ids distinct', p['duplicate_ids'] == __want['duplicate_ids'])\ntry:\n    profile_table([{'id': 'a', 'wert': 3}, {'id': 'b'}])\n    __check('fehlende Spalte -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('fehlende Spalte -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': '3'}])\n    __check('falscher Typ -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('falscher Typ -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': True}])\n    __check('bool -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('bool -> ValueError', True)\n\n"
          },
          "prompt": "Profiltabelle ids p/q/r mit zwei Duplikat-ids und Sentinel -1. Implementiere `profile_table` wie im Vertrag (total/missing/sentinel/duplicate_ids, ValueError bei Schema)."
        },
        {
          "parameters": {
            "tests": "rows = [{'id': 'm1', 'wert': 12}, {'id': 'm2', 'wert': None}, {'id': 'm3', 'wert': None}, {'id': 'm1', 'wert': -1}]\np = profile_table(rows)\nids = [row['id'] for row in rows]\nwerte = [row['wert'] for row in rows]\ncounts = {}\nfor rid in ids:\n    counts[rid] = counts.get(rid, 0) + 1\n__want = {\n    'total': len(rows),\n    'missing': sum(1 for w in werte if w is None),\n    'sentinel': sum(1 for w in werte if w == -1),\n    'duplicate_ids': sum(1 for c in counts.values() if c > 1),\n}\n__check('total', p['total'] == __want['total'])\n__check('missing', p['missing'] == __want['missing'])\n__check('sentinel', p['sentinel'] == __want['sentinel'])\n__check('duplicate_ids distinct', p['duplicate_ids'] == __want['duplicate_ids'])\ntry:\n    profile_table([{'id': 'a', 'wert': 3}, {'id': 'b'}])\n    __check('fehlende Spalte -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('fehlende Spalte -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': '3'}])\n    __check('falscher Typ -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('falscher Typ -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': True}])\n    __check('bool -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('bool -> ValueError', True)\n\n"
          },
          "prompt": "Profiltabelle m1–m3: zwei None, ein Sentinel, Duplikat m1. Implementiere `profile_table` wie im Vertrag (total/missing/sentinel/duplicate_ids, ValueError bei Schema)."
        },
        {
          "parameters": {
            "tests": "rows = [{'id': 's', 'wert': -1}, {'id': 's', 'wert': -1}, {'id': 't', 'wert': 5}]\np = profile_table(rows)\nids = [row['id'] for row in rows]\nwerte = [row['wert'] for row in rows]\ncounts = {}\nfor rid in ids:\n    counts[rid] = counts.get(rid, 0) + 1\n__want = {\n    'total': len(rows),\n    'missing': sum(1 for w in werte if w is None),\n    'sentinel': sum(1 for w in werte if w == -1),\n    'duplicate_ids': sum(1 for c in counts.values() if c > 1),\n}\n__check('total', p['total'] == __want['total'])\n__check('missing', p['missing'] == __want['missing'])\n__check('sentinel', p['sentinel'] == __want['sentinel'])\n__check('duplicate_ids distinct', p['duplicate_ids'] == __want['duplicate_ids'])\ntry:\n    profile_table([{'id': 'a', 'wert': 3}, {'id': 'b'}])\n    __check('fehlende Spalte -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('fehlende Spalte -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': '3'}])\n    __check('falscher Typ -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('falscher Typ -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': True}])\n    __check('bool -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('bool -> ValueError', True)\n\n"
          },
          "prompt": "Profiltabelle s/t: Sentinel doppelt auf id s. Implementiere `profile_table` wie im Vertrag (total/missing/sentinel/duplicate_ids, ValueError bei Schema)."
        },
        {
          "parameters": {
            "tests": "rows = [{'id': 'u', 'wert': 1}, {'id': 'v', 'wert': 2}, {'id': 'w', 'wert': 3}]\np = profile_table(rows)\nids = [row['id'] for row in rows]\nwerte = [row['wert'] for row in rows]\ncounts = {}\nfor rid in ids:\n    counts[rid] = counts.get(rid, 0) + 1\n__want = {\n    'total': len(rows),\n    'missing': sum(1 for w in werte if w is None),\n    'sentinel': sum(1 for w in werte if w == -1),\n    'duplicate_ids': sum(1 for c in counts.values() if c > 1),\n}\n__check('total', p['total'] == __want['total'])\n__check('missing', p['missing'] == __want['missing'])\n__check('sentinel', p['sentinel'] == __want['sentinel'])\n__check('duplicate_ids distinct', p['duplicate_ids'] == __want['duplicate_ids'])\ntry:\n    profile_table([{'id': 'a', 'wert': 3}, {'id': 'b'}])\n    __check('fehlende Spalte -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('fehlende Spalte -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': '3'}])\n    __check('falscher Typ -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('falscher Typ -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': True}])\n    __check('bool -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('bool -> ValueError', True)\n\n"
          },
          "prompt": "Profiltabelle u/v/w ohne Lücken, ohne Sentinel, ohne Duplikate. Implementiere `profile_table` wie im Vertrag (total/missing/sentinel/duplicate_ids, ValueError bei Schema)."
        },
        {
          "parameters": {
            "tests": "rows = [{'id': 'aa', 'wert': None}, {'id': 'bb', 'wert': None}, {'id': 'cc', 'wert': 0}]\np = profile_table(rows)\nids = [row['id'] for row in rows]\nwerte = [row['wert'] for row in rows]\ncounts = {}\nfor rid in ids:\n    counts[rid] = counts.get(rid, 0) + 1\n__want = {\n    'total': len(rows),\n    'missing': sum(1 for w in werte if w is None),\n    'sentinel': sum(1 for w in werte if w == -1),\n    'duplicate_ids': sum(1 for c in counts.values() if c > 1),\n}\n__check('total', p['total'] == __want['total'])\n__check('missing', p['missing'] == __want['missing'])\n__check('sentinel', p['sentinel'] == __want['sentinel'])\n__check('duplicate_ids distinct', p['duplicate_ids'] == __want['duplicate_ids'])\ntry:\n    profile_table([{'id': 'a', 'wert': 3}, {'id': 'b'}])\n    __check('fehlende Spalte -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('fehlende Spalte -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': '3'}])\n    __check('falscher Typ -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('falscher Typ -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': True}])\n    __check('bool -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('bool -> ValueError', True)\n\n"
          },
          "prompt": "Profiltabelle aa/bb/cc: zwei missing, kein Sentinel. Implementiere `profile_table` wie im Vertrag (total/missing/sentinel/duplicate_ids, ValueError bei Schema)."
        },
        {
          "parameters": {
            "tests": "rows = [{'id': 'k', 'wert': -1}, {'id': 'l', 'wert': -1}, {'id': 'n', 'wert': -1}, {'id': 'k', 'wert': 7}]\np = profile_table(rows)\nids = [row['id'] for row in rows]\nwerte = [row['wert'] for row in rows]\ncounts = {}\nfor rid in ids:\n    counts[rid] = counts.get(rid, 0) + 1\n__want = {\n    'total': len(rows),\n    'missing': sum(1 for w in werte if w is None),\n    'sentinel': sum(1 for w in werte if w == -1),\n    'duplicate_ids': sum(1 for c in counts.values() if c > 1),\n}\n__check('total', p['total'] == __want['total'])\n__check('missing', p['missing'] == __want['missing'])\n__check('sentinel', p['sentinel'] == __want['sentinel'])\n__check('duplicate_ids distinct', p['duplicate_ids'] == __want['duplicate_ids'])\ntry:\n    profile_table([{'id': 'a', 'wert': 3}, {'id': 'b'}])\n    __check('fehlende Spalte -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('fehlende Spalte -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': '3'}])\n    __check('falscher Typ -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('falscher Typ -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': True}])\n    __check('bool -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('bool -> ValueError', True)\n\n"
          },
          "prompt": "Profiltabelle k/l/n: drei Sentinel-Werte und Duplikat k. Implementiere `profile_table` wie im Vertrag (total/missing/sentinel/duplicate_ids, ValueError bei Schema)."
        },
        {
          "parameters": {
            "tests": "rows = [{'id': 'g', 'wert': 10}, {'id': 'h', 'wert': None}, {'id': 'g', 'wert': 10}, {'id': 'i', 'wert': -1}, {'id': 'h', 'wert': 1}]\np = profile_table(rows)\nids = [row['id'] for row in rows]\nwerte = [row['wert'] for row in rows]\ncounts = {}\nfor rid in ids:\n    counts[rid] = counts.get(rid, 0) + 1\n__want = {\n    'total': len(rows),\n    'missing': sum(1 for w in werte if w is None),\n    'sentinel': sum(1 for w in werte if w == -1),\n    'duplicate_ids': sum(1 for c in counts.values() if c > 1),\n}\n__check('total', p['total'] == __want['total'])\n__check('missing', p['missing'] == __want['missing'])\n__check('sentinel', p['sentinel'] == __want['sentinel'])\n__check('duplicate_ids distinct', p['duplicate_ids'] == __want['duplicate_ids'])\ntry:\n    profile_table([{'id': 'a', 'wert': 3}, {'id': 'b'}])\n    __check('fehlende Spalte -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('fehlende Spalte -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': '3'}])\n    __check('falscher Typ -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('falscher Typ -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': True}])\n    __check('bool -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('bool -> ValueError', True)\n\n"
          },
          "prompt": "Profiltabelle g/h/i mit zwei Duplikat-ids, einer Lücke, einem Sentinel. Implementiere `profile_table` wie im Vertrag (total/missing/sentinel/duplicate_ids, ValueError bei Schema)."
        },
        {
          "parameters": {
            "tests": "rows = [{'id': 'z9', 'wert': 0}, {'id': 'z9', 'wert': 0}, {'id': 'z8', 'wert': None}, {'id': 'z7', 'wert': -1}, {'id': 'z6', 'wert': 4}, {'id': 'z5', 'wert': -1}]\np = profile_table(rows)\nids = [row['id'] for row in rows]\nwerte = [row['wert'] for row in rows]\ncounts = {}\nfor rid in ids:\n    counts[rid] = counts.get(rid, 0) + 1\n__want = {\n    'total': len(rows),\n    'missing': sum(1 for w in werte if w is None),\n    'sentinel': sum(1 for w in werte if w == -1),\n    'duplicate_ids': sum(1 for c in counts.values() if c > 1),\n}\n__check('total', p['total'] == __want['total'])\n__check('missing', p['missing'] == __want['missing'])\n__check('sentinel', p['sentinel'] == __want['sentinel'])\n__check('duplicate_ids distinct', p['duplicate_ids'] == __want['duplicate_ids'])\ntry:\n    profile_table([{'id': 'a', 'wert': 3}, {'id': 'b'}])\n    __check('fehlende Spalte -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('fehlende Spalte -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': '3'}])\n    __check('falscher Typ -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('falscher Typ -> ValueError', True)\n\ntry:\n    profile_table([{'id': 'a', 'wert': True}])\n    __check('bool -> ValueError', False, 'kein ValueError')\nexcept ValueError:\n    __check('bool -> ValueError', True)\n\n"
          },
          "prompt": "Profiltabelle z9–z5, n=6, Duplikat z9, zwei Sentinel. Implementiere `profile_table` wie im Vertrag (total/missing/sentinel/duplicate_ids, ValueError bei Schema)."
        }
      ]
    },
    {
      "caseId": "validate-rows-contract-errors",
      "difficultyProfile": "stretch",
      "masteryEligible": true,
      "sourceLineage": [
        "w06-e5"
      ],
      "parameters": {
        "packages": [
          "numpy"
        ],
        "starterCode": "def validate_rows(rows, contract):\n    \"\"\"Check rows against the contract; return list of (i, column, kind) errors.\"\"\"\n    # 1) column set per row: mismatch -> (i, None, \"columns\"), skip row\n    # 2) per column in contract order: None/type check, then range\n    # 3) after the row pass: unique columns -> (i, col, \"duplicate\")\n    ...\n",
        "tests": "contract = {\n    \"columns\": [\"id\", \"alter\", \"umsatz\"],\n    \"types\": {\"id\": \"str\", \"alter\": \"int\", \"umsatz\": \"number\"},\n    \"nullable\": [\"umsatz\"],\n    \"ranges\": {\"alter\": [18, 99]},\n    \"unique\": [\"id\"],\n}\nok_rows = [\n    {\"id\": \"k1\", \"alter\": 34, \"umsatz\": 120.5},\n    {\"id\": \"k2\", \"alter\": 41, \"umsatz\": None},\n    {\"id\": \"k3\", \"alter\": 29, \"umsatz\": 90},\n]\n__check('fehlerfreie Tabelle', validate_rows(ok_rows, contract) == [])\nbad_rows = [\n    {\"id\": \"k1\", \"alter\": 34, \"umsatz\": 120.5},\n    {\"id\": \"k2\", \"alter\": \"41\", \"umsatz\": 3.0},\n    {\"id\": \"k3\", \"alter\": 17, \"umsatz\": 2.0},\n    {\"id\": \"k1\", \"alter\": 52, \"umsatz\": None},\n    {\"alter\": 52, \"umsatz\": 1.0},\n    {\"id\": \"k9\", \"umsatz\": 1.0},\n    {\"id\": True, \"alter\": 30, \"umsatz\": 1.0},\n    {\"id\": \"k7\", \"alter\": None, \"umsatz\": 1.0},\n    {\"id\": \"k8\", \"alter\": 30, \"umsatz\": \"viel\"},\n]\nerrs = validate_rows(bad_rows, contract)\n__check('Anzahl Fehler', len(errs) == 8)\n__check('Typfehler alter', (1, \"alter\", \"type\") in errs)\n__check('Bereichsfehler alter', (2, \"alter\", \"range\") in errs)\n__check('Duplikat id', (3, \"id\", \"duplicate\") in errs)\n__check('fehlende Spalte Zeile 4', (4, None, \"columns\") in errs)\n__check('fehlende Spalte Zeile 5', (5, None, \"columns\") in errs)\n__check('bool ist kein str', (6, \"id\", \"type\") in errs)\n__check('None nicht nullable', (7, \"alter\", \"type\") in errs)\n__check('umsatz kein number', (8, \"umsatz\", \"type\") in errs)\n__check('Bereichsgrenze inklusiv', validate_rows([{\"id\": \"k1\", \"alter\": 18, \"umsatz\": 1.0}, {\"id\": \"k2\", \"alter\": 99, \"umsatz\": 1.0}], contract) == [])"
      },
      "expected": {
        "kind": "reference-solver",
        "referenceSolver": "def validate_rows(rows, contract):\n    columns = list(contract[\"columns\"])\n    types = contract.get(\"types\", {})\n    nullable = set(contract.get(\"nullable\", []))\n    ranges = contract.get(\"ranges\", {})\n    errors = []\n    for i, row in enumerate(rows):\n        if not isinstance(row, dict) or set(row.keys()) != set(columns):\n            errors.append((i, None, \"columns\"))\n            continue\n        for col in columns:\n            value = row[col]\n            if value is None:\n                if col not in nullable:\n                    errors.append((i, col, \"type\"))\n                continue\n            expected = types.get(col)\n            ok = True\n            if expected == \"str\":\n                ok = isinstance(value, str)\n            elif expected == \"int\":\n                ok = isinstance(value, int) and not isinstance(value, bool)\n            elif expected == \"number\":\n                ok = isinstance(value, (int, float)) and not isinstance(value, bool)\n            if not ok:\n                errors.append((i, col, \"type\"))\n                continue\n            if col in ranges:\n                lo, hi = ranges[col]\n                if not (lo <= value <= hi):\n                    errors.append((i, col, \"range\"))\n    for col in contract.get(\"unique\", []):\n        seen = set()\n        for i, row in enumerate(rows):\n            if not isinstance(row, dict) or set(row.keys()) != set(columns):\n                continue\n            value = row.get(col)\n            if value in seen:\n                errors.append((i, col, \"duplicate\"))\n            else:\n                seen.add(value)\n    return errors"
      },
      "prompt": "Final Boss Datenqualitätsvertrag: Implementiere <code>validate_rows(rows, contract)</code>. Eingabe: <code>rows</code> ist eine Liste von Dictionaries; <code>contract</code> hat die Schlüssel <code>columns</code> (Liste der erwarteten Spalten — genau diese, keine fehlenden, keine zusätzlichen), <code>types</code> (Abbildung Spalte → <code>\"str\"</code> | <code>\"int\"</code> | <code>\"number\"</code>; <code>\"int\"</code> und <code>\"number\"</code> lehnen bool ab, <code>\"number\"</code> akzeptiert int und float), <code>nullable</code> (Liste der Spalten, in denen None erlaubt ist), <code>ranges</code> (Abbildung Spalte → [min, max] mit inklusiven Grenzen, nur für vorhandene numerische Werte) und <code>unique</code> (Liste der Spalten, deren Werte zeilenweise eindeutig sein müssen). Rückgabe: Liste von Fehler-Tupeln <code>(zeilenindex, spalte, art)</code> mit art aus <code>\"columns\"</code>, <code>\"type\"</code>, <code>\"range\"</code>, <code>\"duplicate\"</code>. Regeln: falsches Spaltenset einer Zeile → genau <code>(i, None, \"columns\")</code>, keine weiteren Prüfungen für diese Zeile; None außerhalb von nullable → <code>(i, spalte, \"type\")</code>; Typ- und Bereichsprüfung erfolgen je Zeile in der Reihenfolge von <code>columns</code>; nach dem Zeilendurchlauf wird jede <code>unique</code>-Spalte geprüft — jede Wiederholung nach dem ersten Auftreten ergibt <code>(i, spalte, \"duplicate\")</code>. Fehlerfreie Eingaben geben <code>[]</code> zurück. Der Testcode enthält eine fehlerfreie Tabelle und eine Tabelle mit allen Fehlerarten.",
      "fullSolution": "def validate_rows(rows, contract):\n    columns = list(contract[\"columns\"])\n    types = contract.get(\"types\", {})\n    nullable = set(contract.get(\"nullable\", []))\n    ranges = contract.get(\"ranges\", {})\n    errors = []\n    for i, row in enumerate(rows):\n        if not isinstance(row, dict) or set(row.keys()) != set(columns):\n            errors.append((i, None, \"columns\"))\n            continue\n        for col in columns:\n            value = row[col]\n            if value is None:\n                if col not in nullable:\n                    errors.append((i, col, \"type\"))\n                continue\n            expected = types.get(col)\n            ok = True\n            if expected == \"str\":\n                ok = isinstance(value, str)\n            elif expected == \"int\":\n                ok = isinstance(value, int) and not isinstance(value, bool)\n            elif expected == \"number\":\n                ok = isinstance(value, (int, float)) and not isinstance(value, bool)\n            if not ok:\n                errors.append((i, col, \"type\"))\n                continue\n            if col in ranges:\n                lo, hi = ranges[col]\n                if not (lo <= value <= hi):\n                    errors.append((i, col, \"range\"))\n    for col in contract.get(\"unique\", []):\n        seen = set()\n        for i, row in enumerate(rows):\n            if not isinstance(row, dict) or set(row.keys()) != set(columns):\n                continue\n            value = row.get(col)\n            if value in seen:\n                errors.append((i, col, \"duplicate\"))\n            else:\n                seen.add(value)\n    return errors\n\n# bad_rows aus dem Test liefern 8 Fehler: type/range/columns/duplicate wie oben spezifiziert",
      "hints": [
        "Arbeite in zwei Durchläufen: erst Zeilen gegen Spaltenset, Typen und Bereiche, danach separat die unique-Spalten mit einem seen-Set.",
        "None ist nur in nullable erlaubt und ergibt sonst einen type-Fehler; bool ist eine Unterklasse von int und muss für \"int\"/\"number\" explizit ausgeschlossen werden."
      ],
      "feedbackRules": [
        {
          "if": "columns-error-missing",
          "then": "Bei falschem Spaltensatz darf die Zeile keine weiteren Fehler liefern — weiter zur nächsten Zeile."
        },
        {
          "if": "duplicate-early",
          "then": "Duplikatfehler werden erst nach dem vollständigen Zeilendurchlauf gemeldet, sonst verschieben sie die Fehlerliste."
        },
        {
          "if": "range-on-none",
          "then": "Bereiche werden nur für vorhandene numerische Werte geprüft — None darf nicht in den Range-Vergleich gehen."
        }
      ],
      "typicalErrors": [
        "bool als int akzeptiert, weil isinstance(True, int) gilt",
        "None in den Bereichs-Vergleich einbezogen und gegen min/max geprüft",
        "Duplikate während des Zeilendurchlaufs gemeldet statt in einem zweiten Durchlauf"
      ],
      "tolerancePolicy": {
        "mode": "tests"
      },
      "activityType": "python-code",
      "graderId": "pyodide",
      "variants": [
        {
          "parameters": {
            "tests": "contract = {'columns': ['sku', 'anzahl', 'preis'], 'types': {'sku': 'str', 'anzahl': 'int', 'preis': 'number'}, 'nullable': ['preis'], 'ranges': {'anzahl': [1, 50]}, 'unique': ['sku']}\nok_rows = [{'sku': 'A1', 'anzahl': 4, 'preis': 2.5}, {'sku': 'B2', 'anzahl': 9, 'preis': None}]\n__check('fehlerfreie Tabelle', validate_rows(ok_rows, contract) == [])\nbad_rows = [{'sku': 'ok1', 'anzahl': 2, 'preis': 1.0}, {'sku': 'ok2', 'anzahl': 'x', 'preis': 1.0}, {'sku': 'ok3', 'anzahl': 0, 'preis': 1.0}, {'sku': 'ok1', 'anzahl': 50, 'preis': None}, {'anzahl': 50, 'preis': 1.0}, {'sku': 'ok9', 'preis': 1.0}, {'sku': True, 'anzahl': 1, 'preis': 1.0}, {'sku': 'ok7', 'anzahl': None, 'preis': 1.0}, {'sku': 'ok8', 'anzahl': 1, 'preis': 'viel'}]\n\ndef __want(rows, contract):\n    columns = list(contract['columns'])\n    types = contract.get('types', {})\n    nullable = set(contract.get('nullable', []))\n    ranges = contract.get('ranges', {})\n    errors = []\n    for i, row in enumerate(rows):\n        if not isinstance(row, dict) or set(row.keys()) != set(columns):\n            errors.append((i, None, 'columns'))\n            continue\n        for col in columns:\n            value = row[col]\n            if value is None:\n                if col not in nullable:\n                    errors.append((i, col, 'type'))\n                continue\n            expected = types.get(col)\n            ok = True\n            if expected == 'str':\n                ok = isinstance(value, str)\n            elif expected == 'int':\n                ok = isinstance(value, int) and not isinstance(value, bool)\n            elif expected == 'number':\n                ok = isinstance(value, (int, float)) and not isinstance(value, bool)\n            if not ok:\n                errors.append((i, col, 'type'))\n                continue\n            if col in ranges:\n                lo, hi = ranges[col]\n                if not (lo <= value <= hi):\n                    errors.append((i, col, 'range'))\n    for col in contract.get('unique', []):\n        seen = set()\n        for i, row in enumerate(rows):\n            if not isinstance(row, dict) or set(row.keys()) != set(columns):\n                continue\n            value = row.get(col)\n            if value in seen:\n                errors.append((i, col, 'duplicate'))\n            else:\n                seen.add(value)\n    return errors\n\nerrs = validate_rows(bad_rows, contract)\n__check('Fehlerliste', errs == __want(bad_rows, contract))\n__check('Anzahl Fehler', len(errs) >= 6)\nbounds = [{ 'sku': 'b1', 'anzahl': 1, 'preis': 1.0 }, { 'sku': 'b2', 'anzahl': 50, 'preis': 1.0 }]\n__check('Bereichsgrenze inklusiv', validate_rows(bounds, contract) == [])\n"
          },
          "prompt": "Vertrag sku/anzahl/preis, Range anzahl 1–50. Implementiere `validate_rows` wie im Vertrag; der Test prüft eine saubere und eine fehlerhafte Tabelle."
        },
        {
          "parameters": {
            "tests": "contract = {'columns': ['name', 'jahr', 'score'], 'types': {'name': 'str', 'jahr': 'int', 'score': 'number'}, 'nullable': ['score'], 'ranges': {'jahr': [2000, 2026]}, 'unique': ['name']}\nok_rows = [{'name': 'alpha', 'jahr': 2011, 'score': 0.4}, {'name': 'beta', 'jahr': 2020, 'score': None}]\n__check('fehlerfreie Tabelle', validate_rows(ok_rows, contract) == [])\nbad_rows = [{'name': 'ok1', 'jahr': 2001, 'score': 1.0}, {'name': 'ok2', 'jahr': 'x', 'score': 1.0}, {'name': 'ok3', 'jahr': 1999, 'score': 1.0}, {'name': 'ok1', 'jahr': 2026, 'score': None}, {'jahr': 2026, 'score': 1.0}, {'name': 'ok9', 'score': 1.0}, {'name': True, 'jahr': 2000, 'score': 1.0}, {'name': 'ok7', 'jahr': None, 'score': 1.0}, {'name': 'ok8', 'jahr': 2000, 'score': 'viel'}]\n\ndef __want(rows, contract):\n    columns = list(contract['columns'])\n    types = contract.get('types', {})\n    nullable = set(contract.get('nullable', []))\n    ranges = contract.get('ranges', {})\n    errors = []\n    for i, row in enumerate(rows):\n        if not isinstance(row, dict) or set(row.keys()) != set(columns):\n            errors.append((i, None, 'columns'))\n            continue\n        for col in columns:\n            value = row[col]\n            if value is None:\n                if col not in nullable:\n                    errors.append((i, col, 'type'))\n                continue\n            expected = types.get(col)\n            ok = True\n            if expected == 'str':\n                ok = isinstance(value, str)\n            elif expected == 'int':\n                ok = isinstance(value, int) and not isinstance(value, bool)\n            elif expected == 'number':\n                ok = isinstance(value, (int, float)) and not isinstance(value, bool)\n            if not ok:\n                errors.append((i, col, 'type'))\n                continue\n            if col in ranges:\n                lo, hi = ranges[col]\n                if not (lo <= value <= hi):\n                    errors.append((i, col, 'range'))\n    for col in contract.get('unique', []):\n        seen = set()\n        for i, row in enumerate(rows):\n            if not isinstance(row, dict) or set(row.keys()) != set(columns):\n                continue\n            value = row.get(col)\n            if value in seen:\n                errors.append((i, col, 'duplicate'))\n            else:\n                seen.add(value)\n    return errors\n\nerrs = validate_rows(bad_rows, contract)\n__check('Fehlerliste', errs == __want(bad_rows, contract))\n__check('Anzahl Fehler', len(errs) >= 6)\nbounds = [{ 'name': 'b1', 'jahr': 2000, 'score': 1.0 }, { 'name': 'b2', 'jahr': 2026, 'score': 1.0 }]\n__check('Bereichsgrenze inklusiv', validate_rows(bounds, contract) == [])\n"
          },
          "prompt": "Vertrag name/jahr/score, Range jahr 2000–2026. Implementiere `validate_rows` wie im Vertrag; der Test prüft eine saubere und eine fehlerhafte Tabelle."
        },
        {
          "parameters": {
            "tests": "contract = {'columns': ['uid', 'alter', 'kg'], 'types': {'uid': 'str', 'alter': 'int', 'kg': 'number'}, 'nullable': ['kg'], 'ranges': {'alter': [10, 80]}, 'unique': ['uid']}\nok_rows = [{'uid': 'u1', 'alter': 22, 'kg': 70.0}, {'uid': 'u2', 'alter': 40, 'kg': None}]\n__check('fehlerfreie Tabelle', validate_rows(ok_rows, contract) == [])\nbad_rows = [{'uid': 'ok1', 'alter': 11, 'kg': 1.0}, {'uid': 'ok2', 'alter': 'x', 'kg': 1.0}, {'uid': 'ok3', 'alter': 9, 'kg': 1.0}, {'uid': 'ok1', 'alter': 80, 'kg': None}, {'alter': 80, 'kg': 1.0}, {'uid': 'ok9', 'kg': 1.0}, {'uid': True, 'alter': 10, 'kg': 1.0}, {'uid': 'ok7', 'alter': None, 'kg': 1.0}, {'uid': 'ok8', 'alter': 10, 'kg': 'viel'}]\n\ndef __want(rows, contract):\n    columns = list(contract['columns'])\n    types = contract.get('types', {})\n    nullable = set(contract.get('nullable', []))\n    ranges = contract.get('ranges', {})\n    errors = []\n    for i, row in enumerate(rows):\n        if not isinstance(row, dict) or set(row.keys()) != set(columns):\n            errors.append((i, None, 'columns'))\n            continue\n        for col in columns:\n            value = row[col]\n            if value is None:\n                if col not in nullable:\n                    errors.append((i, col, 'type'))\n                continue\n            expected = types.get(col)\n            ok = True\n            if expected == 'str':\n                ok = isinstance(value, str)\n            elif expected == 'int':\n                ok = isinstance(value, int) and not isinstance(value, bool)\n            elif expected == 'number':\n                ok = isinstance(value, (int, float)) and not isinstance(value, bool)\n            if not ok:\n                errors.append((i, col, 'type'))\n                continue\n            if col in ranges:\n                lo, hi = ranges[col]\n                if not (lo <= value <= hi):\n                    errors.append((i, col, 'range'))\n    for col in contract.get('unique', []):\n        seen = set()\n        for i, row in enumerate(rows):\n            if not isinstance(row, dict) or set(row.keys()) != set(columns):\n                continue\n            value = row.get(col)\n            if value in seen:\n                errors.append((i, col, 'duplicate'))\n            else:\n                seen.add(value)\n    return errors\n\nerrs = validate_rows(bad_rows, contract)\n__check('Fehlerliste', errs == __want(bad_rows, contract))\n__check('Anzahl Fehler', len(errs) >= 6)\nbounds = [{ 'uid': 'b1', 'alter': 10, 'kg': 1.0 }, { 'uid': 'b2', 'alter': 80, 'kg': 1.0 }]\n__check('Bereichsgrenze inklusiv', validate_rows(bounds, contract) == [])\n"
          },
          "prompt": "Vertrag uid/alter/kg, Range alter 10–80. Implementiere `validate_rows` wie im Vertrag; der Test prüft eine saubere und eine fehlerhafte Tabelle."
        },
        {
          "parameters": {
            "tests": "contract = {'columns': ['code', 'n', 'rate'], 'types': {'code': 'str', 'n': 'int', 'rate': 'number'}, 'nullable': ['rate'], 'ranges': {'n': [0, 10]}, 'unique': ['code']}\nok_rows = [{'code': 'c0', 'n': 0, 'rate': 0.1}, {'code': 'c1', 'n': 10, 'rate': None}]\n__check('fehlerfreie Tabelle', validate_rows(ok_rows, contract) == [])\nbad_rows = [{'code': 'ok1', 'n': 1, 'rate': 1.0}, {'code': 'ok2', 'n': 'x', 'rate': 1.0}, {'code': 'ok3', 'n': -1, 'rate': 1.0}, {'code': 'ok1', 'n': 10, 'rate': None}, {'n': 10, 'rate': 1.0}, {'code': 'ok9', 'rate': 1.0}, {'code': True, 'n': 0, 'rate': 1.0}, {'code': 'ok7', 'n': None, 'rate': 1.0}, {'code': 'ok8', 'n': 0, 'rate': 'viel'}]\n\ndef __want(rows, contract):\n    columns = list(contract['columns'])\n    types = contract.get('types', {})\n    nullable = set(contract.get('nullable', []))\n    ranges = contract.get('ranges', {})\n    errors = []\n    for i, row in enumerate(rows):\n        if not isinstance(row, dict) or set(row.keys()) != set(columns):\n            errors.append((i, None, 'columns'))\n            continue\n        for col in columns:\n            value = row[col]\n            if value is None:\n                if col not in nullable:\n                    errors.append((i, col, 'type'))\n                continue\n            expected = types.get(col)\n            ok = True\n            if expected == 'str':\n                ok = isinstance(value, str)\n            elif expected == 'int':\n                ok = isinstance(value, int) and not isinstance(value, bool)\n            elif expected == 'number':\n                ok = isinstance(value, (int, float)) and not isinstance(value, bool)\n            if not ok:\n                errors.append((i, col, 'type'))\n                continue\n            if col in ranges:\n                lo, hi = ranges[col]\n                if not (lo <= value <= hi):\n                    errors.append((i, col, 'range'))\n    for col in contract.get('unique', []):\n        seen = set()\n        for i, row in enumerate(rows):\n            if not isinstance(row, dict) or set(row.keys()) != set(columns):\n                continue\n            value = row.get(col)\n            if value in seen:\n                errors.append((i, col, 'duplicate'))\n            else:\n                seen.add(value)\n    return errors\n\nerrs = validate_rows(bad_rows, contract)\n__check('Fehlerliste', errs == __want(bad_rows, contract))\n__check('Anzahl Fehler', len(errs) >= 6)\nbounds = [{ 'code': 'b1', 'n': 0, 'rate': 1.0 }, { 'code': 'b2', 'n': 10, 'rate': 1.0 }]\n__check('Bereichsgrenze inklusiv', validate_rows(bounds, contract) == [])\n"
          },
          "prompt": "Vertrag code/n/rate, Range n 0–10 inklusiv. Implementiere `validate_rows` wie im Vertrag; der Test prüft eine saubere und eine fehlerhafte Tabelle."
        },
        {
          "parameters": {
            "tests": "contract = {'columns': ['id', 'stufe', 'wert'], 'types': {'id': 'str', 'stufe': 'int', 'wert': 'number'}, 'nullable': ['wert'], 'ranges': {'stufe': [1, 5]}, 'unique': ['id']}\nok_rows = [{'id': 'i1', 'stufe': 1, 'wert': 3.0}, {'id': 'i2', 'stufe': 5, 'wert': None}]\n__check('fehlerfreie Tabelle', validate_rows(ok_rows, contract) == [])\nbad_rows = [{'id': 'ok1', 'stufe': 2, 'wert': 1.0}, {'id': 'ok2', 'stufe': 'x', 'wert': 1.0}, {'id': 'ok3', 'stufe': 0, 'wert': 1.0}, {'id': 'ok1', 'stufe': 5, 'wert': None}, {'stufe': 5, 'wert': 1.0}, {'id': 'ok9', 'wert': 1.0}, {'id': True, 'stufe': 1, 'wert': 1.0}, {'id': 'ok7', 'stufe': None, 'wert': 1.0}, {'id': 'ok8', 'stufe': 1, 'wert': 'viel'}]\n\ndef __want(rows, contract):\n    columns = list(contract['columns'])\n    types = contract.get('types', {})\n    nullable = set(contract.get('nullable', []))\n    ranges = contract.get('ranges', {})\n    errors = []\n    for i, row in enumerate(rows):\n        if not isinstance(row, dict) or set(row.keys()) != set(columns):\n            errors.append((i, None, 'columns'))\n            continue\n        for col in columns:\n            value = row[col]\n            if value is None:\n                if col not in nullable:\n                    errors.append((i, col, 'type'))\n                continue\n            expected = types.get(col)\n            ok = True\n            if expected == 'str':\n                ok = isinstance(value, str)\n            elif expected == 'int':\n                ok = isinstance(value, int) and not isinstance(value, bool)\n            elif expected == 'number':\n                ok = isinstance(value, (int, float)) and not isinstance(value, bool)\n            if not ok:\n                errors.append((i, col, 'type'))\n                continue\n            if col in ranges:\n                lo, hi = ranges[col]\n                if not (lo <= value <= hi):\n                    errors.append((i, col, 'range'))\n    for col in contract.get('unique', []):\n        seen = set()\n        for i, row in enumerate(rows):\n            if not isinstance(row, dict) or set(row.keys()) != set(columns):\n                continue\n            value = row.get(col)\n            if value in seen:\n                errors.append((i, col, 'duplicate'))\n            else:\n                seen.add(value)\n    return errors\n\nerrs = validate_rows(bad_rows, contract)\n__check('Fehlerliste', errs == __want(bad_rows, contract))\n__check('Anzahl Fehler', len(errs) >= 6)\nbounds = [{ 'id': 'b1', 'stufe': 1, 'wert': 1.0 }, { 'id': 'b2', 'stufe': 5, 'wert': 1.0 }]\n__check('Bereichsgrenze inklusiv', validate_rows(bounds, contract) == [])\n"
          },
          "prompt": "Vertrag id/stufe/wert, Range stufe 1–5. Implementiere `validate_rows` wie im Vertrag; der Test prüft eine saubere und eine fehlerhafte Tabelle."
        },
        {
          "parameters": {
            "tests": "contract = {'columns': ['tid', 'qty', 'eur'], 'types': {'tid': 'str', 'qty': 'int', 'eur': 'number'}, 'nullable': ['eur'], 'ranges': {'qty': [2, 9]}, 'unique': ['tid']}\nok_rows = [{'tid': 't9', 'qty': 2, 'eur': 1.0}, {'tid': 't8', 'qty': 9, 'eur': None}]\n__check('fehlerfreie Tabelle', validate_rows(ok_rows, contract) == [])\nbad_rows = [{'tid': 'ok1', 'qty': 3, 'eur': 1.0}, {'tid': 'ok2', 'qty': 'x', 'eur': 1.0}, {'tid': 'ok3', 'qty': 1, 'eur': 1.0}, {'tid': 'ok1', 'qty': 9, 'eur': None}, {'qty': 9, 'eur': 1.0}, {'tid': 'ok9', 'eur': 1.0}, {'tid': True, 'qty': 2, 'eur': 1.0}, {'tid': 'ok7', 'qty': None, 'eur': 1.0}, {'tid': 'ok8', 'qty': 2, 'eur': 'viel'}]\n\ndef __want(rows, contract):\n    columns = list(contract['columns'])\n    types = contract.get('types', {})\n    nullable = set(contract.get('nullable', []))\n    ranges = contract.get('ranges', {})\n    errors = []\n    for i, row in enumerate(rows):\n        if not isinstance(row, dict) or set(row.keys()) != set(columns):\n            errors.append((i, None, 'columns'))\n            continue\n        for col in columns:\n            value = row[col]\n            if value is None:\n                if col not in nullable:\n                    errors.append((i, col, 'type'))\n                continue\n            expected = types.get(col)\n            ok = True\n            if expected == 'str':\n                ok = isinstance(value, str)\n            elif expected == 'int':\n                ok = isinstance(value, int) and not isinstance(value, bool)\n            elif expected == 'number':\n                ok = isinstance(value, (int, float)) and not isinstance(value, bool)\n            if not ok:\n                errors.append((i, col, 'type'))\n                continue\n            if col in ranges:\n                lo, hi = ranges[col]\n                if not (lo <= value <= hi):\n                    errors.append((i, col, 'range'))\n    for col in contract.get('unique', []):\n        seen = set()\n        for i, row in enumerate(rows):\n            if not isinstance(row, dict) or set(row.keys()) != set(columns):\n                continue\n            value = row.get(col)\n            if value in seen:\n                errors.append((i, col, 'duplicate'))\n            else:\n                seen.add(value)\n    return errors\n\nerrs = validate_rows(bad_rows, contract)\n__check('Fehlerliste', errs == __want(bad_rows, contract))\n__check('Anzahl Fehler', len(errs) >= 6)\nbounds = [{ 'tid': 'b1', 'qty': 2, 'eur': 1.0 }, { 'tid': 'b2', 'qty': 9, 'eur': 1.0 }]\n__check('Bereichsgrenze inklusiv', validate_rows(bounds, contract) == [])\n"
          },
          "prompt": "Vertrag tid/qty/eur, Range qty 2–9. Implementiere `validate_rows` wie im Vertrag; der Test prüft eine saubere und eine fehlerhafte Tabelle."
        },
        {
          "parameters": {
            "tests": "contract = {'columns': ['hid', 'mm', 'temp'], 'types': {'hid': 'str', 'mm': 'int', 'temp': 'number'}, 'nullable': ['temp'], 'ranges': {'mm': [0, 365]}, 'unique': ['hid']}\nok_rows = [{'hid': 'h1', 'mm': 0, 'temp': 12.5}, {'hid': 'h2', 'mm': 365, 'temp': None}]\n__check('fehlerfreie Tabelle', validate_rows(ok_rows, contract) == [])\nbad_rows = [{'hid': 'ok1', 'mm': 1, 'temp': 1.0}, {'hid': 'ok2', 'mm': 'x', 'temp': 1.0}, {'hid': 'ok3', 'mm': -1, 'temp': 1.0}, {'hid': 'ok1', 'mm': 365, 'temp': None}, {'mm': 365, 'temp': 1.0}, {'hid': 'ok9', 'temp': 1.0}, {'hid': True, 'mm': 0, 'temp': 1.0}, {'hid': 'ok7', 'mm': None, 'temp': 1.0}, {'hid': 'ok8', 'mm': 0, 'temp': 'viel'}]\n\ndef __want(rows, contract):\n    columns = list(contract['columns'])\n    types = contract.get('types', {})\n    nullable = set(contract.get('nullable', []))\n    ranges = contract.get('ranges', {})\n    errors = []\n    for i, row in enumerate(rows):\n        if not isinstance(row, dict) or set(row.keys()) != set(columns):\n            errors.append((i, None, 'columns'))\n            continue\n        for col in columns:\n            value = row[col]\n            if value is None:\n                if col not in nullable:\n                    errors.append((i, col, 'type'))\n                continue\n            expected = types.get(col)\n            ok = True\n            if expected == 'str':\n                ok = isinstance(value, str)\n            elif expected == 'int':\n                ok = isinstance(value, int) and not isinstance(value, bool)\n            elif expected == 'number':\n                ok = isinstance(value, (int, float)) and not isinstance(value, bool)\n            if not ok:\n                errors.append((i, col, 'type'))\n                continue\n            if col in ranges:\n                lo, hi = ranges[col]\n                if not (lo <= value <= hi):\n                    errors.append((i, col, 'range'))\n    for col in contract.get('unique', []):\n        seen = set()\n        for i, row in enumerate(rows):\n            if not isinstance(row, dict) or set(row.keys()) != set(columns):\n                continue\n            value = row.get(col)\n            if value in seen:\n                errors.append((i, col, 'duplicate'))\n            else:\n                seen.add(value)\n    return errors\n\nerrs = validate_rows(bad_rows, contract)\n__check('Fehlerliste', errs == __want(bad_rows, contract))\n__check('Anzahl Fehler', len(errs) >= 6)\nbounds = [{ 'hid': 'b1', 'mm': 0, 'temp': 1.0 }, { 'hid': 'b2', 'mm': 365, 'temp': 1.0 }]\n__check('Bereichsgrenze inklusiv', validate_rows(bounds, contract) == [])\n"
          },
          "prompt": "Vertrag hid/mm/temp, Range mm 0–365. Implementiere `validate_rows` wie im Vertrag; der Test prüft eine saubere und eine fehlerhafte Tabelle."
        },
        {
          "parameters": {
            "tests": "contract = {'columns': ['pid', 'k', 'p'], 'types': {'pid': 'str', 'k': 'int', 'p': 'number'}, 'nullable': ['p'], 'ranges': {'k': [3, 12]}, 'unique': ['pid']}\nok_rows = [{'pid': 'pA', 'k': 3, 'p': 0.0}, {'pid': 'pB', 'k': 12, 'p': None}]\n__check('fehlerfreie Tabelle', validate_rows(ok_rows, contract) == [])\nbad_rows = [{'pid': 'ok1', 'k': 4, 'p': 1.0}, {'pid': 'ok2', 'k': 'x', 'p': 1.0}, {'pid': 'ok3', 'k': 2, 'p': 1.0}, {'pid': 'ok1', 'k': 12, 'p': None}, {'k': 12, 'p': 1.0}, {'pid': 'ok9', 'p': 1.0}, {'pid': True, 'k': 3, 'p': 1.0}, {'pid': 'ok7', 'k': None, 'p': 1.0}, {'pid': 'ok8', 'k': 3, 'p': 'viel'}]\n\ndef __want(rows, contract):\n    columns = list(contract['columns'])\n    types = contract.get('types', {})\n    nullable = set(contract.get('nullable', []))\n    ranges = contract.get('ranges', {})\n    errors = []\n    for i, row in enumerate(rows):\n        if not isinstance(row, dict) or set(row.keys()) != set(columns):\n            errors.append((i, None, 'columns'))\n            continue\n        for col in columns:\n            value = row[col]\n            if value is None:\n                if col not in nullable:\n                    errors.append((i, col, 'type'))\n                continue\n            expected = types.get(col)\n            ok = True\n            if expected == 'str':\n                ok = isinstance(value, str)\n            elif expected == 'int':\n                ok = isinstance(value, int) and not isinstance(value, bool)\n            elif expected == 'number':\n                ok = isinstance(value, (int, float)) and not isinstance(value, bool)\n            if not ok:\n                errors.append((i, col, 'type'))\n                continue\n            if col in ranges:\n                lo, hi = ranges[col]\n                if not (lo <= value <= hi):\n                    errors.append((i, col, 'range'))\n    for col in contract.get('unique', []):\n        seen = set()\n        for i, row in enumerate(rows):\n            if not isinstance(row, dict) or set(row.keys()) != set(columns):\n                continue\n            value = row.get(col)\n            if value in seen:\n                errors.append((i, col, 'duplicate'))\n            else:\n                seen.add(value)\n    return errors\n\nerrs = validate_rows(bad_rows, contract)\n__check('Fehlerliste', errs == __want(bad_rows, contract))\n__check('Anzahl Fehler', len(errs) >= 6)\nbounds = [{ 'pid': 'b1', 'k': 3, 'p': 1.0 }, { 'pid': 'b2', 'k': 12, 'p': 1.0 }]\n__check('Bereichsgrenze inklusiv', validate_rows(bounds, contract) == [])\n"
          },
          "prompt": "Vertrag pid/k/p, Range k 3–12. Implementiere `validate_rows` wie im Vertrag; der Test prüft eine saubere und eine fehlerhafte Tabelle."
        },
        {
          "parameters": {
            "tests": "contract = {'columns': ['rid', 'jahrgang', 'note'], 'types': {'rid': 'str', 'jahrgang': 'int', 'note': 'number'}, 'nullable': ['note'], 'ranges': {'jahrgang': [1990, 2010]}, 'unique': ['rid']}\nok_rows = [{'rid': 'r1', 'jahrgang': 1990, 'note': 1.0}, {'rid': 'r2', 'jahrgang': 2010, 'note': None}]\n__check('fehlerfreie Tabelle', validate_rows(ok_rows, contract) == [])\nbad_rows = [{'rid': 'ok1', 'jahrgang': 1991, 'note': 1.0}, {'rid': 'ok2', 'jahrgang': 'x', 'note': 1.0}, {'rid': 'ok3', 'jahrgang': 1989, 'note': 1.0}, {'rid': 'ok1', 'jahrgang': 2010, 'note': None}, {'jahrgang': 2010, 'note': 1.0}, {'rid': 'ok9', 'note': 1.0}, {'rid': True, 'jahrgang': 1990, 'note': 1.0}, {'rid': 'ok7', 'jahrgang': None, 'note': 1.0}, {'rid': 'ok8', 'jahrgang': 1990, 'note': 'viel'}]\n\ndef __want(rows, contract):\n    columns = list(contract['columns'])\n    types = contract.get('types', {})\n    nullable = set(contract.get('nullable', []))\n    ranges = contract.get('ranges', {})\n    errors = []\n    for i, row in enumerate(rows):\n        if not isinstance(row, dict) or set(row.keys()) != set(columns):\n            errors.append((i, None, 'columns'))\n            continue\n        for col in columns:\n            value = row[col]\n            if value is None:\n                if col not in nullable:\n                    errors.append((i, col, 'type'))\n                continue\n            expected = types.get(col)\n            ok = True\n            if expected == 'str':\n                ok = isinstance(value, str)\n            elif expected == 'int':\n                ok = isinstance(value, int) and not isinstance(value, bool)\n            elif expected == 'number':\n                ok = isinstance(value, (int, float)) and not isinstance(value, bool)\n            if not ok:\n                errors.append((i, col, 'type'))\n                continue\n            if col in ranges:\n                lo, hi = ranges[col]\n                if not (lo <= value <= hi):\n                    errors.append((i, col, 'range'))\n    for col in contract.get('unique', []):\n        seen = set()\n        for i, row in enumerate(rows):\n            if not isinstance(row, dict) or set(row.keys()) != set(columns):\n                continue\n            value = row.get(col)\n            if value in seen:\n                errors.append((i, col, 'duplicate'))\n            else:\n                seen.add(value)\n    return errors\n\nerrs = validate_rows(bad_rows, contract)\n__check('Fehlerliste', errs == __want(bad_rows, contract))\n__check('Anzahl Fehler', len(errs) >= 6)\nbounds = [{ 'rid': 'b1', 'jahrgang': 1990, 'note': 1.0 }, { 'rid': 'b2', 'jahrgang': 2010, 'note': 1.0 }]\n__check('Bereichsgrenze inklusiv', validate_rows(bounds, contract) == [])\n"
          },
          "prompt": "Vertrag rid/jahrgang/note, Range jahrgang 1990–2010. Implementiere `validate_rows` wie im Vertrag; der Test prüft eine saubere und eine fehlerhafte Tabelle."
        }
      ]
    }
  ]
}
