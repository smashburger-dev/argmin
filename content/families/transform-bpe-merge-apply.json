{
  "schemaVersion": 1,
  "familyId": "transform-bpe-merge-apply",
  "contract": {
    "familyId": "transform-bpe-merge-apply",
    "familyGroup": "transform-terms",
    "summary": "Wendet eine BPE-Mergetabelle in Tabellenreihenfolge mit links-nach-rechts nicht überlappenden Merges auf eine Symbolfolge an.",
    "taskArchetype": "code-tests",
    "authorityMode": "static",
    "masteryEligible": true,
    "caseTypes": [
      {
        "caseId": "bpe-merge-apply",
        "propertyTest": false
      },
      {
        "caseId": "bpe-merge-tie-order",
        "propertyTest": false
      }
    ],
    "difficultyProfiles": [
      "stretch",
      "core"
    ],
    "competencyIds": [
      "c-dl-tokenizer",
      "c-python-collections"
    ],
    "graderId": "pyodide",
    "activityType": "python-code"
  },
  "cases": [
    {
      "caseId": "bpe-merge-apply",
      "difficultyProfile": "stretch",
      "masteryEligible": true,
      "sourceLineage": [
        "w23-e5"
      ],
      "parameters": {
        "packages": [
          "numpy"
        ],
        "starterCode": "def bpe_encode(word, merges):\n    \"\"\"Apply the merge table in order: each merge hits all occurrences (left-to-right, non-overlapping).\"\"\"\n    # start from list(word) + [\"</w>\"]\n    # for each pair in table order: single left-to-right pass merging all occurrences\n    ...\n",
        "tests": "MERGES = [(\"l\", \"o\"), (\"lo\", \"w\"), (\"e\", \"s\"), (\"es\", \"t\")]\n__check('lowest mit Lerntabelle', bpe_encode(\"lowest\", MERGES) == [\"low\", \"est\", \"</w>\"])\n__check('low mit Lerntabelle', bpe_encode(\"low\", MERGES) == [\"low\", \"</w>\"])\n__check('lower mit Lerntabelle', bpe_encode(\"lower\", MERGES) == [\"low\", \"e\", \"r\", \"</w>\"])\n__check('leere Tabelle reine Zeichen', bpe_encode(\"ab\", []) == [\"a\", \"b\", \"</w>\"])\n__check('unbekanntes Wort zerfaellt', bpe_encode(\"cab\", MERGES) == [\"c\", \"a\", \"b\", \"</w>\"])\n__check('Overlapping von links', bpe_encode(\"aaa\", [(\"a\", \"a\")]) == [\"aa\", \"a\", \"</w>\"])\n__check('Tabellenreihenfolge wirkt', bpe_encode(\"lowest\", [(\"e\", \"s\"), (\"l\", \"o\")]) == [\"lo\", \"w\", \"es\", \"t\", \"</w>\"])\n__check('Tokenzahl gesunken', len(bpe_encode(\"lowest\", MERGES)) == 3)\n__check('Marker immer letztes Symbol', bpe_encode(\"lowest\", MERGES)[-1] == \"</w>\")"
      },
      "competencyIds": [
        "c-dl-tokenizer",
        "c-python-collections"
      ],
      "expected": {
        "kind": "reference-solver",
        "referenceSolver": "def bpe_encode(word, merges):\n    \"\"\"Apply the merge table in order: each merge hits all occurrences (left-to-right, non-overlapping).\"\"\"\n    symbols = list(word) + [\"</w>\"]\n    for pair in merges:\n        a, b = pair\n        out = []\n        i = 0\n        while i < len(symbols):\n            if i + 1 < len(symbols) and symbols[i] == a and symbols[i + 1] == b:\n                out.append(a + b)\n                i += 2\n            else:\n                out.append(symbols[i])\n                i += 1\n        symbols = out\n    return symbols\n"
      },
      "prompt": "BPE anwenden: Implementiere <code>bpe_encode(word, merges)</code>. Vertrag: Das Wort startet als Zeichenliste plus End-of-Word-Marker <code>\"&lt;/w&gt;\"</code>; danach gehst du die Mergetabelle <strong>in Tabellenreihenfolge</strong> durch und wendest jeden Merge <strong>einmal</strong> auf alle Vorkommen in der aktuellen Symbolfolge an — von links nach rechts, überlappende Paare zählen nicht doppelt (aus <code>a a a</code> wird <code>aa a</code>). Rückgabe: die finale Symbolfolge als Liste von Strings. Der Testcode nutzt eine vorgegebene Beispiel-Mergetabelle (l o, lo w, e s, es t) und prüft Known-Wörter, unbekannte Wörter, Tabellenreihenfolge und Überlappungen.",
      "fullSolution": "def bpe_encode(word, merges):\n    \"\"\"Apply the merge table in order: each merge hits all occurrences (left-to-right, non-overlapping).\"\"\"\n    symbols = list(word) + [\"</w>\"]\n    for pair in merges:\n        a, b = pair\n        out = []\n        i = 0\n        while i < len(symbols):\n            if i + 1 < len(symbols) and symbols[i] == a and symbols[i + 1] == b:\n                out.append(a + b)\n                i += 2\n            else:\n                out.append(symbols[i])\n                i += 1\n        symbols = out\n    return symbols\n# bpe_encode(\"lowest\", [(\"l\",\"o\"),(\"lo\",\"w\"),(\"e\",\"s\"),(\"es\",\"t\")])\n# -> [\"low\", \"est\", \"</w>\"]  (aus 7 Zeichen-Tokens werden 3 Symbole).",
      "hints": [
        "Symbole als Liste von Strings führen; der Marker ist das letzte Startsymbol.",
        "Pro Merge ein linker nach rechter Durchlauf mit Positionsindex: bei Treffer beide Symbole zum verschmolzenen String zusammenfassen und Index um 2 weiterschalten."
      ],
      "feedbackRules": [
        {
          "if": "table-reordered",
          "then": "Die Tabellenreihenfolge ist Priorität — nicht nach Häufigkeit oder Länge sortieren."
        },
        {
          "if": "overlapping-merged",
          "then": "Überlappende Vorkommen werden nicht doppelt gezählt: nach einem Merge springt der Index um 2."
        },
        {
          "if": "marker-missing",
          "then": "Der End-of-Word-Marker gehört zur Startfolge und bleibt das letzte Symbol."
        }
      ],
      "typicalErrors": [
        "Mergetabelle umsortiert (Häufigkeit statt Reihenfolge)",
        "überlappende Vorkommen doppelt verschmolzen",
        "End-of-Word-Marker unterschlagen"
      ],
      "tolerancePolicy": {
        "mode": "tests"
      },
      "activityType": "python-code",
      "graderId": "pyodide"
    },
    {
      "caseId": "bpe-merge-tie-order",
      "difficultyProfile": "stretch",
      "masteryEligible": true,
      "sourceLineage": [
        "c1-authored"
      ],
      "parameters": {
        "packages": [],
        "starterCode": "def apply_merges(symbols, merges):\n    ...\n",
        "tests": "__check('Tie folgt Tabellenreihenfolge', apply_merges([\"a\", \"b\", \"c\"], [(\"a\", \"b\"), (\"b\", \"c\")]) == [\"ab\", \"c\"])\n__check('zweiter Merge sieht erstes Ergebnis', apply_merges([\"a\", \"b\", \"c\"], [(\"a\", \"b\"), (\"ab\", \"c\")]) == [\"abc\"])\n__check('nicht ueberlappend', apply_merges([\"a\", \"a\", \"a\"], [(\"a\", \"a\")]) == [\"aa\", \"a\"])\n__check('kein Merge unveraendert', apply_merges([\"x\", \"y\"], [(\"a\", \"b\")]) == [\"x\", \"y\"])\n"
      },
      "competencyIds": [
        "c-dl-tokenizer",
        "c-python-collections"
      ],
      "expected": {
        "kind": "reference-solver",
        "referenceSolver": "def apply_merges(symbols, merges):\n    symbols = list(symbols)\n    for left, right in merges:\n        out = []\n        i = 0\n        while i < len(symbols):\n            if i + 1 < len(symbols) and symbols[i] == left and symbols[i + 1] == right:\n                out.append(left + right)\n                i += 2\n            else:\n                out.append(symbols[i])\n                i += 1\n        symbols = out\n    return symbols\n"
      },
      "prompt": "Wende eine geordnete BPE-Mergetabelle auf eine Symbolfolge an. Bei einem Tie entscheidet die Tabellenreihenfolge; Treffer werden links-nach-rechts und nicht überlappend zusammengeführt.",
      "fullSolution": "Jeder Merge läuft über die aktuelle Symbolfolge. Ein Treffer erzeugt ein kombiniertes Symbol und springt um zwei Positionen weiter; danach wird der nächste Merge auf die neue Folge angewendet.",
      "hints": [
        "Arbeite mit einer neuen Ausgabeliste pro Mergerunde.",
        "Nach einem Treffer zwei Eingabesymbole verbrauchen; so wird aa a nicht zu einem überlappenden Dreifach-Merge."
      ],
      "feedbackRules": [
        {
          "if": "table-reordered",
          "then": "Bei [a,b,c] und den zwei Kandidaten wird zuerst (a,b) nach Tabellenreihenfolge verarbeitet, nicht der zweite Pair-Kandidat."
        },
        {
          "if": "overlapping-merged",
          "then": "Die Folge [a,a,a] ergibt mit (a,a) genau [aa,a], weil das erste Paar die nächste Position überspringt."
        },
        {
          "if": "new-pair-skipped",
          "then": "Nach (a,b) entsteht ab; der folgende Merge (ab,c) darf dieses neue Symbol in der nächsten Runde verwenden."
        }
      ],
      "typicalErrors": [
        "Mergetabelle nach eigener Häufigkeitsannahme sortiert",
        "überlappende Symbole mehrfach im selben Durchlauf verbraucht",
        "neu entstandenes Paar nicht in der nächsten Mergerunde berücksichtigt"
      ],
      "tolerancePolicy": {
        "mode": "tests"
      },
      "activityType": "python-code",
      "graderId": "pyodide"
    }
  ]
}
