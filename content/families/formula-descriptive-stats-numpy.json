{
  "schemaVersion": 1,
  "familyId": "formula-descriptive-stats-numpy",
  "contract": null,
  "cases": [
    {
      "caseId": "describe-and-bins",
      "difficultyProfile": "core",
      "masteryEligible": true,
      "sourceLineage": [
        "w07-e4"
      ],
      "parameters": {
        "packages": [
          "numpy"
        ],
        "starterCode": "import numpy as np\n\n\ndef describe(values):\n    \"\"\"Return {'n', 'mean', 'median', 'std'} with population std (ddof=0).\"\"\"\n    ...\n\n\ndef bin_counts(values, edges):\n    \"\"\"Return counts per bin with np.histogram edge rules (last edge inclusive).\"\"\"\n    ...\n",
        "tests": "d = describe([2, 4, 4, 4, 5, 5, 7, 9])\n__check('n', d[\"n\"] == 8)\n__check('mean', abs(d[\"mean\"] - 5.0) < 1e-9)\n__check('median', abs(d[\"median\"] - 4.5) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std([2, 4, 4, 4, 5, 5, 7, 9]))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts([2, 4, 4, 4, 5, 5, 7, 9], [0, 4, 9]), np.array([1, 7])))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([5.0, 9.0, 9.0], [0, 5, 9]), np.array([0, 3])))\n__check('leerer Bin', np.array_equal(bin_counts([1.0, 8.0], [0, 2, 4, 9]), np.array([1, 0, 1])))"
      },
      "expected": {
        "kind": "reference-solver",
        "referenceSolver": "import numpy as np\n\ndef describe(values):\n    values = np.asarray(values, dtype=float)\n    return {\n        \"n\": int(values.size),\n        \"mean\": float(np.mean(values)),\n        \"median\": float(np.median(values)),\n        \"std\": float(np.std(values)),\n    }\n\n\ndef bin_counts(values, edges):\n    return np.histogram(np.asarray(values), bins=np.asarray(edges, dtype=float))[0]"
      },
      "prompt": "Implementiere zwei Zusammenfassungs-Funktionen mit NumPy. <code>describe(values)</code> bekommt eine Liste oder ein Array von Zahlen und gibt ein Dictionary mit <code>\"n\"</code> (Anzahl der Werte, int), <code>\"mean\"</code>, <code>\"median\"</code> und <code>\"std\"</code> zurück (floats; Standardabweichung als Bevölkerungswert, also <code>ddof=0</code> wie im NumPy-Standard). <code>bin_counts(values, edges)</code> bekommt Werte und Bin-Kanten und gibt die Anzahlen pro Bin zurück — mit den Kantenregeln von <code>np.histogram</code>: halboffene Bins $[e_k, e_{k+1})$, nur der letzte Bin schließt die rechte Kante ein. Der Testcode prüft beide Funktionen getrennt von deiner Eingabe.",
      "fullSolution": "def describe(values):\n    values = np.asarray(values, dtype=float)\n    return {\n        \"n\": int(values.size),\n        \"mean\": float(np.mean(values)),\n        \"median\": float(np.median(values)),\n        \"std\": float(np.std(values)),\n    }\n\n\ndef bin_counts(values, edges):\n    return np.histogram(np.asarray(values), bins=np.asarray(edges, dtype=float))[0]\n\n# describe([2, 4, 4, 4, 5, 5, 7, 9]) -> {'n': 8, 'mean': 5.0, 'median': 4.5, 'std': 2.0}\n# bin_counts([2, 4, 4, 4, 5, 5, 7, 9], [0, 4, 9]) -> array([1, 7])",
      "hints": [
        "Erst mit np.asarray(values, dtype=float) normalisieren, dann die Aggregationen — .size liefert n, np.std nutzt standardmäßig ddof=0.",
        "bin_counts muss die Kantenregeln von np.histogram treffen: bins-Argument als Kanten übergeben und die Anzahlen ([0]-Index) zurückgeben."
      ],
      "feedbackRules": [
        {
          "if": "std-ddof-wrong",
          "then": "Sample-Standardabweichung mit ddof=1 ist hier falsch — gefragt ist der Bevölkerungswert ddof=0."
        },
        {
          "if": "bin-edges-inclusive",
          "then": "Nur der letzte Bin schließt die rechte Kante ein: [0, 4) und [4, 9] — die 4 zählt in den zweiten Bin."
        },
        {
          "if": "n-type-wrong",
          "then": "\"n\" muss ein Python-int sein (int(...)), kein NumPy-Skalar, falls der Test auf == 8 mit Typ prüft."
        }
      ],
      "typicalErrors": [
        "ddof=1 statt ddof=0 verwendet",
        "Bin-Kanten als inklusive Intervalle links und rechts gelesen",
        "len(values) statt .size nach der Array-Umwandlung gemischt"
      ],
      "tolerancePolicy": {
        "mode": "tests"
      },
      "variants": [
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [1, 3, 3, 5, 9]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 3, 9]), np.histogram(arr, bins=np.asarray([0, 3, 9], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([3.0, 9.0, 9.0], [0, 3, 9]), np.histogram(np.asarray([3.0, 9.0, 9.0], dtype=float), bins=np.asarray([0, 3, 9], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([0.5, 8.0], [0, 2, 5, 9]), np.histogram(np.asarray([0.5, 8.0], dtype=float), bins=np.asarray([0, 2, 5, 9], dtype=float))[0]))\n"
          },
          "prompt": "Messreihe [1, 3, 3, 5, 9] (n=5). Implementiere `describe` und `bin_counts` wie im Vertrag; die Tests prüfen Mittelwert, Median, Bevölkerung-Std und Histogrammkanten [0, 3, 9]."
        },
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [0, 2, 2, 2, 10]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 2, 10]), np.histogram(arr, bins=np.asarray([0, 2, 10], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([2.0, 10.0], [0, 2, 10]), np.histogram(np.asarray([2.0, 10.0], dtype=float), bins=np.asarray([0, 2, 10], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([1.0, 9.0], [0, 3, 6, 10]), np.histogram(np.asarray([1.0, 9.0], dtype=float), bins=np.asarray([0, 3, 6, 10], dtype=float))[0]))\n"
          },
          "prompt": "Messreihe [0, 2, 2, 2, 10] (n=5). Implementiere `describe` und `bin_counts` wie im Vertrag; die Tests prüfen Mittelwert, Median, Bevölkerung-Std und Histogrammkanten [0, 2, 10]."
        },
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [4, 4, 6, 8, 12]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 5, 12]), np.histogram(arr, bins=np.asarray([0, 5, 12], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([5.0, 12.0, 12.0], [0, 5, 12]), np.histogram(np.asarray([5.0, 12.0, 12.0], dtype=float), bins=np.asarray([0, 5, 12], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([0.0, 11.0], [0, 2, 8, 12]), np.histogram(np.asarray([0.0, 11.0], dtype=float), bins=np.asarray([0, 2, 8, 12], dtype=float))[0]))\n"
          },
          "prompt": "Messreihe [4, 4, 6, 8, 12] (n=5). Implementiere `describe` und `bin_counts` wie im Vertrag; die Tests prüfen Mittelwert, Median, Bevölkerung-Std und Histogrammkanten [0, 5, 12]."
        },
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [3, 5, 7, 7, 11]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 7, 11]), np.histogram(arr, bins=np.asarray([0, 7, 11], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([7.0, 11.0], [0, 7, 11]), np.histogram(np.asarray([7.0, 11.0], dtype=float), bins=np.asarray([0, 7, 11], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([1.0, 10.0], [0, 4, 8, 12]), np.histogram(np.asarray([1.0, 10.0], dtype=float), bins=np.asarray([0, 4, 8, 12], dtype=float))[0]))\n"
          },
          "prompt": "Messreihe [3, 5, 7, 7, 11] (n=5). Implementiere `describe` und `bin_counts` wie im Vertrag; die Tests prüfen Mittelwert, Median, Bevölkerung-Std und Histogrammkanten [0, 7, 11]."
        },
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [6, 6, 6, 8, 14]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 6, 14]), np.histogram(arr, bins=np.asarray([0, 6, 14], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([6.0, 14.0, 14.0], [0, 6, 14]), np.histogram(np.asarray([6.0, 14.0, 14.0], dtype=float), bins=np.asarray([0, 6, 14], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([2.0, 13.0], [0, 5, 9, 14]), np.histogram(np.asarray([2.0, 13.0], dtype=float), bins=np.asarray([0, 5, 9, 14], dtype=float))[0]))\n"
          },
          "prompt": "Messreihe [6, 6, 6, 8, 14] (n=5). Implementiere `describe` und `bin_counts` wie im Vertrag; die Tests prüfen Mittelwert, Median, Bevölkerung-Std und Histogrammkanten [0, 6, 14]."
        },
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [1, 1, 4, 8, 8]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 4, 8]), np.histogram(arr, bins=np.asarray([0, 4, 8], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([4.0, 8.0], [0, 4, 8]), np.histogram(np.asarray([4.0, 8.0], dtype=float), bins=np.asarray([0, 4, 8], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([0.5, 7.5], [0, 2, 5, 9]), np.histogram(np.asarray([0.5, 7.5], dtype=float), bins=np.asarray([0, 2, 5, 9], dtype=float))[0]))\n"
          },
          "prompt": "Messreihe [1, 1, 4, 8, 8] (n=5). Implementiere `describe` und `bin_counts` wie im Vertrag; die Tests prüfen Mittelwert, Median, Bevölkerung-Std und Histogrammkanten [0, 4, 8]."
        },
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [2, 6, 6, 10, 18]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 6, 18]), np.histogram(arr, bins=np.asarray([0, 6, 18], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([6.0, 18.0, 18.0], [0, 6, 18]), np.histogram(np.asarray([6.0, 18.0, 18.0], dtype=float), bins=np.asarray([0, 6, 18], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([1.0, 17.0], [0, 3, 12, 18]), np.histogram(np.asarray([1.0, 17.0], dtype=float), bins=np.asarray([0, 3, 12, 18], dtype=float))[0]))\n"
          },
          "prompt": "Messreihe [2, 6, 6, 10, 18] (n=5). Implementiere `describe` und `bin_counts` wie im Vertrag; die Tests prüfen Mittelwert, Median, Bevölkerung-Std und Histogrammkanten [0, 6, 18]."
        },
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [5, 5, 5, 5, 15]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 5, 15]), np.histogram(arr, bins=np.asarray([0, 5, 15], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([5.0, 15.0], [0, 5, 15]), np.histogram(np.asarray([5.0, 15.0], dtype=float), bins=np.asarray([0, 5, 15], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([1.0, 14.0], [0, 4, 10, 16]), np.histogram(np.asarray([1.0, 14.0], dtype=float), bins=np.asarray([0, 4, 10, 16], dtype=float))[0]))\n"
          },
          "prompt": "Messreihe [5, 5, 5, 5, 15] (n=5). Implementiere `describe` und `bin_counts` wie im Vertrag; die Tests prüfen Mittelwert, Median, Bevölkerung-Std und Histogrammkanten [0, 5, 15]."
        },
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [0, 1, 8, 8, 16]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 8, 16]), np.histogram(arr, bins=np.asarray([0, 8, 16], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([8.0, 16.0, 16.0], [0, 8, 16]), np.histogram(np.asarray([8.0, 16.0, 16.0], dtype=float), bins=np.asarray([0, 8, 16], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([2.0, 15.0], [0, 3, 9, 16]), np.histogram(np.asarray([2.0, 15.0], dtype=float), bins=np.asarray([0, 3, 9, 16], dtype=float))[0]))\n"
          },
          "prompt": "Messreihe [0, 1, 8, 8, 16] (n=5). Implementiere `describe` und `bin_counts` wie im Vertrag; die Tests prüfen Mittelwert, Median, Bevölkerung-Std und Histogrammkanten [0, 8, 16]."
        }
      ]
    },
    {
      "caseId": "describe-outlier-bins",
      "difficultyProfile": "challenge",
      "masteryEligible": true,
      "sourceLineage": [
        "c1-authored"
      ],
      "parameters": {
        "packages": [
          "numpy"
        ],
        "starterCode": "import numpy as np\n\ndef describe(values):\n    ...\n\ndef bin_counts(values, edges):\n    ...\n",
        "tests": "d = describe([1, 2, 2, 4, 8])\n__check(\"n\", d[\"n\"] == 5)\n__check(\"mean\", abs(d[\"mean\"] - 3.4) < 1e-9)\n__check(\"median\", abs(d[\"median\"] - 2.0) < 1e-9)\n__check(\"std\", abs(d[\"std\"] - float(np.std([1, 2, 2, 4, 8]))) < 1e-12)\n__check(\"bins\", np.array_equal(bin_counts([1, 2, 2, 4, 8], [0, 2, 4, 8]), np.array([1, 2, 2])))\n__check(\"last edge\", np.array_equal(bin_counts([4.0, 8.0], [0, 4, 8]), np.array([0, 2])))"
      },
      "expected": {
        "kind": "reference-solver",
        "referenceSolver": "import numpy as np\n\ndef describe(values):\n    values = np.asarray(values, dtype=float)\n    return {\"n\": int(values.size), \"mean\": float(np.mean(values)), \"median\": float(np.median(values)), \"std\": float(np.std(values))}\n\ndef bin_counts(values, edges):\n    return np.histogram(np.asarray(values), bins=np.asarray(edges, dtype=float))[0]"
      },
      "prompt": "Implementiere `describe(values)` und `bin_counts(values, edges)` wie im bestehenden Vertrag, aber prüfe deine Lösung an einer asymmetrischen kleinen Stichprobe mit einem Ausreißer und drei Histogramm-Bins.",
      "fullSolution": "def describe(values):\n    values = np.asarray(values, dtype=float)\n    return {\"n\": int(values.size), \"mean\": float(np.mean(values)), \"median\": float(np.median(values)), \"std\": float(np.std(values))}\n\ndef bin_counts(values, edges):\n    return np.histogram(np.asarray(values), bins=np.asarray(edges, dtype=float))[0]",
      "hints": [
        "Normalisiere zuerst in ein Float-Array; `np.std` nutzt bereits `ddof=0`.",
        "Übergebe die Kanten unverändert an `np.histogram`; nur die letzte rechte Kante ist inklusive."
      ],
      "feedbackRules": [
        {
          "if": "wrong-summary",
          "then": "Die vier Kennzahlen müssen aus demselben normalisierten Array berechnet werden."
        },
        {
          "if": "wrong-ddof",
          "then": "Der Vertrag verlangt die Populations-Standardabweichung mit ddof=0."
        },
        {
          "if": "wrong-bins",
          "then": "Die Bin-Grenzen folgen den Regeln von np.histogram."
        }
      ],
      "typicalErrors": [
        "Mittelwert statt Median verwechselt",
        "ddof=1 verwendet",
        "rechte Bin-Kante falsch behandelt"
      ],
      "tolerancePolicy": {
        "mode": "tests"
      },
      "activityType": "python-code",
      "graderId": "pyodide",
      "variants": [
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [0, 1, 1, 3, 12]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 1, 3, 12]), np.histogram(arr, bins=np.asarray([0, 1, 3, 12], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([1.0, 12.0], [0, 1, 12]), np.histogram(np.asarray([1.0, 12.0], dtype=float), bins=np.asarray([0, 1, 12], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([0.0, 11.0], [0, 2, 4, 12]), np.histogram(np.asarray([0.0, 11.0], dtype=float), bins=np.asarray([0, 2, 4, 12], dtype=float))[0]))\n"
          },
          "prompt": "Kleine Stichprobe mit Ausreißer [0, 1, 1, 3, 12]. Dieselbe `describe`/`bin_counts`-Semantik, drei Bins mit Kanten [0, 1, 3, 12]."
        },
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [2, 2, 3, 9, 20]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 3, 9, 20]), np.histogram(arr, bins=np.asarray([0, 3, 9, 20], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([3.0, 20.0], [0, 3, 20]), np.histogram(np.asarray([3.0, 20.0], dtype=float), bins=np.asarray([0, 3, 20], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([2.0, 19.0], [0, 2, 4, 20]), np.histogram(np.asarray([2.0, 19.0], dtype=float), bins=np.asarray([0, 2, 4, 20], dtype=float))[0]))\n"
          },
          "prompt": "Kleine Stichprobe mit Ausreißer [2, 2, 3, 9, 20]. Dieselbe `describe`/`bin_counts`-Semantik, drei Bins mit Kanten [0, 3, 9, 20]."
        },
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [1, 4, 4, 5, 18]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 4, 6, 18]), np.histogram(arr, bins=np.asarray([0, 4, 6, 18], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([4.0, 18.0], [0, 4, 18]), np.histogram(np.asarray([4.0, 18.0], dtype=float), bins=np.asarray([0, 4, 18], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([1.0, 17.0], [0, 2, 4, 18]), np.histogram(np.asarray([1.0, 17.0], dtype=float), bins=np.asarray([0, 2, 4, 18], dtype=float))[0]))\n"
          },
          "prompt": "Kleine Stichprobe mit Ausreißer [1, 4, 4, 5, 18]. Dieselbe `describe`/`bin_counts`-Semantik, drei Bins mit Kanten [0, 4, 6, 18]."
        },
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [3, 3, 3, 6, 15]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 3, 6, 15]), np.histogram(arr, bins=np.asarray([0, 3, 6, 15], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([3.0, 15.0], [0, 3, 15]), np.histogram(np.asarray([3.0, 15.0], dtype=float), bins=np.asarray([0, 3, 15], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([3.0, 14.0], [0, 2, 4, 15]), np.histogram(np.asarray([3.0, 14.0], dtype=float), bins=np.asarray([0, 2, 4, 15], dtype=float))[0]))\n"
          },
          "prompt": "Kleine Stichprobe mit Ausreißer [3, 3, 3, 6, 15]. Dieselbe `describe`/`bin_counts`-Semantik, drei Bins mit Kanten [0, 3, 6, 15]."
        },
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [0, 2, 5, 5, 14]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 2, 5, 14]), np.histogram(arr, bins=np.asarray([0, 2, 5, 14], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([2.0, 14.0], [0, 2, 14]), np.histogram(np.asarray([2.0, 14.0], dtype=float), bins=np.asarray([0, 2, 14], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([0.0, 13.0], [0, 2, 4, 14]), np.histogram(np.asarray([0.0, 13.0], dtype=float), bins=np.asarray([0, 2, 4, 14], dtype=float))[0]))\n"
          },
          "prompt": "Kleine Stichprobe mit Ausreißer [0, 2, 5, 5, 14]. Dieselbe `describe`/`bin_counts`-Semantik, drei Bins mit Kanten [0, 2, 5, 14]."
        },
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [1, 1, 7, 7, 19]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 1, 7, 19]), np.histogram(arr, bins=np.asarray([0, 1, 7, 19], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([1.0, 19.0], [0, 1, 19]), np.histogram(np.asarray([1.0, 19.0], dtype=float), bins=np.asarray([0, 1, 19], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([1.0, 18.0], [0, 2, 4, 19]), np.histogram(np.asarray([1.0, 18.0], dtype=float), bins=np.asarray([0, 2, 4, 19], dtype=float))[0]))\n"
          },
          "prompt": "Kleine Stichprobe mit Ausreißer [1, 1, 7, 7, 19]. Dieselbe `describe`/`bin_counts`-Semantik, drei Bins mit Kanten [0, 1, 7, 19]."
        },
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [4, 4, 5, 8, 22]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 5, 8, 22]), np.histogram(arr, bins=np.asarray([0, 5, 8, 22], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([5.0, 22.0], [0, 5, 22]), np.histogram(np.asarray([5.0, 22.0], dtype=float), bins=np.asarray([0, 5, 22], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([4.0, 21.0], [0, 2, 4, 22]), np.histogram(np.asarray([4.0, 21.0], dtype=float), bins=np.asarray([0, 2, 4, 22], dtype=float))[0]))\n"
          },
          "prompt": "Kleine Stichprobe mit Ausreißer [4, 4, 5, 8, 22]. Dieselbe `describe`/`bin_counts`-Semantik, drei Bins mit Kanten [0, 5, 8, 22]."
        },
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [2, 3, 3, 10, 16]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 3, 10, 16]), np.histogram(arr, bins=np.asarray([0, 3, 10, 16], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([3.0, 16.0], [0, 3, 16]), np.histogram(np.asarray([3.0, 16.0], dtype=float), bins=np.asarray([0, 3, 16], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([2.0, 15.0], [0, 2, 4, 16]), np.histogram(np.asarray([2.0, 15.0], dtype=float), bins=np.asarray([0, 2, 4, 16], dtype=float))[0]))\n"
          },
          "prompt": "Kleine Stichprobe mit Ausreißer [2, 3, 3, 10, 16]. Dieselbe `describe`/`bin_counts`-Semantik, drei Bins mit Kanten [0, 3, 10, 16]."
        },
        {
          "parameters": {
            "tests": "import numpy as np\nvals = [1, 2, 8, 8, 21]\nd = describe(vals)\narr = np.asarray(vals, dtype=float)\n__check('n', d[\"n\"] == int(arr.size))\n__check('mean', abs(d[\"mean\"] - float(np.mean(arr))) < 1e-9)\n__check('median', abs(d[\"median\"] - float(np.median(arr))) < 1e-9)\n__check('std ddof=0', abs(d[\"std\"] - float(np.std(arr))) < 1e-12)\n__check('histogramm bins', np.array_equal(bin_counts(vals, [0, 2, 8, 21]), np.histogram(arr, bins=np.asarray([0, 2, 8, 21], dtype=float))[0]))\n__check('letzte Kante inklusiv', np.array_equal(bin_counts([2.0, 21.0], [0, 2, 21]), np.histogram(np.asarray([2.0, 21.0], dtype=float), bins=np.asarray([0, 2, 21], dtype=float))[0]))\n__check('leerer Bin', np.array_equal(bin_counts([1.0, 20.0], [0, 2, 4, 21]), np.histogram(np.asarray([1.0, 20.0], dtype=float), bins=np.asarray([0, 2, 4, 21], dtype=float))[0]))\n"
          },
          "prompt": "Kleine Stichprobe mit Ausreißer [1, 2, 8, 8, 21]. Dieselbe `describe`/`bin_counts`-Semantik, drei Bins mit Kanten [0, 2, 8, 21]."
        }
      ]
    }
  ]
}
